# ノートパソコン側（クライアント）セットアップ完全ガイド

サーバー側が準備できたら、ノートパソコン側でクライアントをセットアップします。

---

## 📋 目次

1. [Pythonのインストール確認](#pythonのインストール確認)
2. [プロジェクトファイルの準備](#プロジェクトファイルの準備)
3. [必要なライブラリのインストール](#必要なライブラリのインストール)
4. [デスクトップ側の情報を確認](#デスクトップ側の情報を確認)
5. [クライアントを起動して接続](#クライアントを起動して接続)
6. [トラブルシューティング](#トラブルシューティング)

---

## ✅ ステップ1: Pythonのインストール確認

### 1-1. Pythonがインストールされているか確認

1. **コマンドプロンプトを開く**
   - Windowsキー + R → 「cmd」と入力 → Enter

2. **以下を実行**:
   ```bash
   python --version
   ```

3. **結果を確認**:
   - ✅ **Pythonのバージョンが表示される場合**（例: `Python 3.13.9`）
     → 次のステップに進んでください
   - ❌ **「'python' は、内部コマンドまたは外部コマンド...」と表示される場合**
     → Pythonをインストールする必要があります

### 1-2. Pythonがインストールされていない場合

1. **Pythonをダウンロード**
   - https://www.python.org/downloads/ にアクセス
   - 「Download Python」ボタンをクリック

2. **インストール**
   - ダウンロードしたファイルを実行
   - ⚠️ **重要**: 「**Add Python to PATH**」に**必ずチェックを入れる**
   - 「Install Now」をクリック

3. **コマンドプロンプトを再起動**
   - 既に開いているコマンドプロンプトを閉じて、再度開く

4. **確認**:
   ```bash
   python --version
   ```

---

## 📁 ステップ2: プロジェクトファイルの準備

### 方法A: デスクトップ側からファイルをコピー（推奨）

1. **デスクトップ側でプロジェクトフォルダを開く**
   - `C:\Users\西村康成\WasherInspection`

2. **以下のファイルをUSBメモリやクラウドストレージにコピー**:
   - `chrome_like_remote_desktop.py`（必須）
   - `start_chrome_like_client.bat`（オプション、推奨）
   - `check_chrome_like_setup.py`（オプション）
   - `check_setup.bat`（オプション）

3. **ノートパソコン側にコピー**
   - USBメモリやクラウドストレージから、ノートパソコンの任意のフォルダにコピー
   - 例: `C:\Users\[ノートパソコンのユーザー名]\Desktop\RemoteDesktop`

### 方法B: GitHubからクローン（上級者向け）

プロジェクトがGitHubにアップロードされている場合:

```bash
git clone [リポジトリURL]
```

---

## 📦 ステップ3: 必要なライブラリのインストール

### 3-1. プロジェクトフォルダに移動

1. **エクスプローラーでプロジェクトフォルダを開く**
   - ファイルをコピーしたフォルダ（例: `C:\Users\[ユーザー名]\Desktop\RemoteDesktop`）

2. **アドレスバーに「cmd」と入力してEnter**
   - そのフォルダでコマンドプロンプトが開きます

または、コマンドプロンプトで:

```bash
cd "C:\Users\[ユーザー名]\Desktop\RemoteDesktop"
```
（実際のフォルダパスに変更してください）

### 3-2. ライブラリをインストール

1. **pipをアップグレード**（推奨）:
   ```bash
   python -m pip install --upgrade pip
   ```

2. **必要なライブラリをインストール**:
   ```bash
   python -m pip install mss pillow pyautogui pyperclip cryptography
   ```

3. **インストールが完了するまで待つ**
   - 「Successfully installed...」と表示されれば完了

### 3-3. インストール確認（オプション）

```bash
python -m pip list | findstr "mss pillow pyautogui pyperclip cryptography"
```

すべてのライブラリが表示されればOKです。

---

## 🔍 ステップ4: デスクトップ側の情報を確認

クライアントを起動する前に、デスクトップ側の以下の情報が必要です:

### 4-1. IPアドレス

1. **デスクトップ側でコマンドプロンプトを開く**

2. **以下を実行**:
   ```bash
   ipconfig
   ```

3. **IPv4アドレスをメモ**
   - 「IPv4 アドレス」の横に表示される数字
   - 例: `192.168.1.100`

### 4-2. パスワード

1. **デスクトップ側のサーバー起動画面を確認**
   - サーバー起動時に表示されたパスワードをメモ
   - 例: `xYz123AbC456DeF`

2. **サーバーが起動していることを確認**
   - 「[サーバー] 待機中: 0.0.0.0:8888」と表示されているか確認

---

## 🚀 ステップ5: クライアントを起動して接続

### 5-1. クライアントを起動

#### 方法A: バッチファイルから起動（簡単）

1. **エクスプローラーでプロジェクトフォルダを開く**

2. **`start_chrome_like_client.bat` をダブルクリック**
   - GUIが開きます

#### 方法B: コマンドプロンプトから起動

1. **プロジェクトフォルダでコマンドプロンプトを開く**

2. **以下を実行**（IPアドレスを変更してください）:
   ```bash
   python chrome_like_remote_desktop.py client 192.168.1.100
   ```
   - `192.168.1.100` の部分を、デスクトップ側のIPアドレスに変更

3. **GUIが開きます**

#### 方法C: GUIモードから起動

1. **コマンドプロンプトで以下を実行**:
   ```bash
   python chrome_like_remote_desktop.py
   ```

2. **「クライアント（ノートパソコン側）」ボタンをクリック**

---

### 5-2. 接続設定

1. **GUIが開いたら、ツールバーの「サーバー」欄にIPアドレスを入力**
   - 例: `192.168.1.100`

2. **「ポート」欄を確認**（通常は `8888` のまま）

3. **「接続」ボタンをクリック**

---

### 5-3. パスワード認証

1. **パスワード入力ダイアログが表示されます**

2. **デスクトップ側で表示されたパスワードを入力**
   - 例: `xYz123AbC456DeF`
   - ⚠️ **大文字小文字を区別します**

3. **「OK」ボタンをクリック**

4. **認証が成功すると、リモート画面が表示されます**

---

## 🎮 接続後の使い方

### 基本的な操作

- **マウス操作**: リモート画面上でマウスを動かすと、デスクトップ側のマウスが動きます
- **クリック**: 左クリック、右クリック、ダブルクリックが可能
- **キーボード入力**: リモート画面にフォーカスを当てて、キーボードで入力できます
- **スクロール**: マウスホイールでスクロールできます

### ファイル転送

- **ツールバーの「送信」ボタン**: ノートパソコンからデスクトップへファイルを送信
- **ツールバーの「受信」ボタン**: デスクトップからノートパソコンへファイルを受信

### クリップボード共有

- 自動的に同期されます
- ノートパソコン側でコピーしたテキストが、デスクトップ側のクリップボードに自動的にコピーされます

---

## 🔧 トラブルシューティング

### 問題1: 「サーバーに接続できませんでした」と表示される

#### 確認事項

1. **デスクトップ側のサーバーが起動しているか**
   - デスクトップ側のコマンドプロンプトで「[サーバー] 待機中...」と表示されているか確認

2. **IPアドレスが正しいか**
   - デスクトップ側で `ipconfig` を実行して、IPv4アドレスを再確認
   - クライアント側で入力したIPアドレスと一致しているか確認

3. **同じネットワークに接続されているか**
   - 両方のPCが同じWiFiまたは有線LANに接続されているか確認

#### 解決方法

1. **デスクトップ側のファイアウォール設定を確認**
   - Windowsセキュリティ → ファイアウォールとネットワーク保護
   - 「詳細設定」→「受信の規則」→ ポート8888が許可されているか確認

2. **サーバーを再起動**
   - デスクトップ側でサーバーを停止（Ctrl+C）して、再度起動

3. **クライアントを再起動**

---

### 問題2: 「認証に失敗しました」と表示される

#### 確認事項

1. **パスワードが正しいか**
   - デスクトップ側で表示されたパスワードを**正確に**入力しているか確認
   - 大文字小文字を区別します
   - スペースや改行が入っていないか確認

2. **パスワードが古くないか**
   - サーバーを再起動すると、パスワードが変わります
   - 最新のパスワードを使用しているか確認

#### 解決方法

1. **デスクトップ側でパスワードを再確認**
   - サーバー起動時に表示されたパスワードを確認

2. **クライアント側で再度接続**
   - 正しいパスワードを入力

---

### 問題3: ライブラリのインストールエラー

#### 解決方法

1. **pipをアップグレード**:
   ```bash
   python -m pip install --upgrade pip
   ```

2. **個別にインストール**:
   ```bash
   python -m pip install mss
   python -m pip install pillow
   python -m pip install pyautogui
   python -m pip install pyperclip
   python -m pip install cryptography
   ```

3. **管理者権限で実行**:
   - コマンドプロンプトを右クリック → 「管理者として実行」

---

### 問題4: ファイルが見つからない

#### 解決方法

1. **プロジェクトフォルダに移動しているか確認**:
   ```bash
   cd
   ```
   - 正しいフォルダにいるか確認

2. **ファイルが存在するか確認**:
   ```bash
   dir chrome_like_remote_desktop.py
   ```

3. **フルパスで実行**:
   ```bash
   python "C:\Users\[ユーザー名]\Desktop\RemoteDesktop\chrome_like_remote_desktop.py" client 192.168.1.100
   ```

---

## 📝 チェックリスト

### ノートパソコン側の準備

- [ ] Pythonがインストールされている（`python --version` で確認）
- [ ] プロジェクトファイルをコピーした
- [ ] 必要なライブラリをインストールした
- [ ] セットアップ確認スクリプトを実行した（オプション）

### 接続前の確認

- [ ] デスクトップ側のIPアドレスをメモした
- [ ] デスクトップ側のパスワードをメモした
- [ ] デスクトップ側のサーバーが起動していることを確認した
- [ ] 両方のPCが同じネットワークに接続されていることを確認した

### 接続

- [ ] クライアントを起動した
- [ ] IPアドレスを入力した
- [ ] 「接続」ボタンをクリックした
- [ ] パスワードを入力した
- [ ] リモート画面が表示された

---

## 🎯 まとめ

1. **Pythonのインストール確認**: `python --version`
2. **プロジェクトファイルの準備**: デスクトップ側からコピー
3. **ライブラリのインストール**: `python -m pip install mss pillow pyautogui pyperclip cryptography`
4. **デスクトップ側の情報確認**: IPアドレスとパスワード
5. **クライアント起動**: `python chrome_like_remote_desktop.py client [IPアドレス]`
6. **接続**: IPアドレスを入力して「接続」→ パスワードを入力

---

**問題が解決しない場合は、エラーメッセージを確認して、上記のトラブルシューティングを参照してください。**



