# ノートパソコン側 - 接続までの最終ステップ

ライブラリのインストールが完了したら、次は接続です。

---

## 📋 接続前の準備

### ステップ1: プロジェクトファイルを準備

デスクトップ側から、ノートパソコンに以下のファイルをコピーしてください:

- **`chrome_like_remote_desktop.py`**（必須）
- `start_chrome_like_client.bat`（オプション、推奨）

**コピー方法**:
- USBメモリ
- クラウドストレージ（OneDrive、Google Driveなど）
- ネットワーク共有

**コピー先**: ノートパソコンの任意のフォルダ
- 例: `C:\Users\kouse\Desktop\RemoteDesktop`

---

### ステップ2: デスクトップ側の情報を確認

接続に必要な情報を確認してください:

#### 2-1. IPアドレスを確認

**デスクトップ側で**:

1. **コマンドプロンプトを開く**

2. **以下を実行**:
   ```bash
   ipconfig
   ```

3. **IPv4アドレスをメモ**
   - 「IPv4 アドレス」の横に表示される数字
   - 例: `192.168.1.100`

#### 2-2. パスワードを確認

**デスクトップ側のサーバー起動画面で**:

- サーバー起動時に表示されたパスワードをメモ
- 例: `xYz123AbC456DeF`

#### 2-3. サーバーが起動していることを確認

**デスクトップ側のコマンドプロンプトで**:

- 「[サーバー] 待機中: 0.0.0.0:8888」と表示されているか確認
- このウィンドウは閉じないでください

---

## 🚀 クライアントを起動して接続

### ステップ3: プロジェクトフォルダに移動

1. **エクスプローラーでプロジェクトフォルダを開く**
   - ファイルをコピーしたフォルダ（例: `C:\Users\kouse\Desktop\RemoteDesktop`）

2. **アドレスバーに「cmd」と入力してEnter**
   - そのフォルダでコマンドプロンプトが開きます

または、コマンドプロンプトで:

```bash
cd "C:\Users\kouse\Desktop\RemoteDesktop"
```
（実際のフォルダパスに変更してください）

---

### ステップ4: クライアントを起動

#### 方法A: バッチファイルから起動（簡単）

1. **エクスプローラーでプロジェクトフォルダを開く**

2. **`start_chrome_like_client.bat` をダブルクリック**
   - GUIが開きます

#### 方法B: コマンドプロンプトから起動

1. **プロジェクトフォルダでコマンドプロンプトを開く**

2. **以下を実行**（IPアドレスを変更してください）:
   ```bash
   python chrome_like_remote_desktop.py client 192.168.1.100
   ```
   - `192.168.1.100` の部分を、デスクトップ側のIPアドレスに変更

3. **GUIが開きます**

---

### ステップ5: 接続設定

1. **GUIが開いたら、ツールバーの「サーバー」欄にIPアドレスを入力**
   - 例: `192.168.1.100`

2. **「ポート」欄を確認**（通常は `8888` のまま）

3. **「接続」ボタンをクリック**

---

### ステップ6: パスワード認証

1. **パスワード入力ダイアログが表示されます**

2. **デスクトップ側で表示されたパスワードを入力**
   - 例: `xYz123AbC456DeF`
   - ⚠️ **大文字小文字を区別します**

3. **「OK」ボタンをクリック**

4. **認証が成功すると、リモート画面が表示されます**

---

## ✅ 接続成功！

リモート画面が表示されれば、接続成功です！

### 基本的な操作

- **マウス操作**: リモート画面上でマウスを動かすと、デスクトップ側のマウスが動きます
- **クリック**: 左クリック、右クリック、ダブルクリックが可能
- **キーボード入力**: リモート画面にフォーカスを当てて、キーボードで入力できます
- **スクロール**: マウスホイールでスクロールできます

### ファイル転送

- **ツールバーの「送信」ボタン**: ノートパソコンからデスクトップへファイルを送信
- **ツールバーの「受信」ボタン**: デスクトップからノートパソコンへファイルを受信

### クリップボード共有

- 自動的に同期されます

---

## 🔧 トラブルシューティング

### 問題1: 「サーバーに接続できませんでした」と表示される

#### 確認事項

1. **デスクトップ側のサーバーが起動しているか**
   - デスクトップ側のコマンドプロンプトで「[サーバー] 待機中...」と表示されているか確認

2. **IPアドレスが正しいか**
   - デスクトップ側で `ipconfig` を実行して、IPv4アドレスを再確認
   - クライアント側で入力したIPアドレスと一致しているか確認

3. **同じネットワークに接続されているか**
   - 両方のPCが同じWiFiまたは有線LANに接続されているか確認

#### 解決方法

1. **デスクトップ側のファイアウォール設定を確認**
   - Windowsセキュリティ → ファイアウォールとネットワーク保護
   - 「詳細設定」→「受信の規則」→ ポート8888が許可されているか確認

2. **サーバーを再起動**
   - デスクトップ側でサーバーを停止（Ctrl+C）して、再度起動

3. **クライアントを再起動**

---

### 問題2: 「認証に失敗しました」と表示される

#### 確認事項

1. **パスワードが正しいか**
   - デスクトップ側で表示されたパスワードを**正確に**入力しているか確認
   - 大文字小文字を区別します
   - スペースや改行が入っていないか確認

2. **パスワードが古くないか**
   - サーバーを再起動すると、パスワードが変わります
   - 最新のパスワードを使用しているか確認

#### 解決方法

1. **デスクトップ側でパスワードを再確認**
   - サーバー起動時に表示されたパスワードを確認

2. **クライアント側で再度接続**
   - 正しいパスワードを入力

---

### 問題3: ファイルが見つからない

#### 解決方法

1. **プロジェクトフォルダに移動しているか確認**:
   ```bash
   cd
   ```
   - 正しいフォルダにいるか確認

2. **ファイルが存在するか確認**:
   ```bash
   dir chrome_like_remote_desktop.py
   ```

3. **フルパスで実行**:
   ```bash
   python "C:\Users\kouse\Desktop\RemoteDesktop\chrome_like_remote_desktop.py" client 192.168.1.100
   ```

---

## 📝 チェックリスト

### 接続前の確認

- [ ] プロジェクトファイル（`chrome_like_remote_desktop.py`）をコピーした
- [ ] デスクトップ側のIPアドレスをメモした
- [ ] デスクトップ側のパスワードをメモした
- [ ] デスクトップ側のサーバーが起動していることを確認した
- [ ] 両方のPCが同じネットワークに接続されていることを確認した

### 接続

- [ ] クライアントを起動した
- [ ] IPアドレスを入力した
- [ ] 「接続」ボタンをクリックした
- [ ] パスワードを入力した
- [ ] リモート画面が表示された

---

## 🎯 まとめ

1. **プロジェクトファイルをコピー**: `chrome_like_remote_desktop.py`
2. **デスクトップ側の情報を確認**: IPアドレスとパスワード
3. **クライアントを起動**: `python chrome_like_remote_desktop.py client [IPアドレス]`
4. **接続**: IPアドレスを入力して「接続」→ パスワードを入力
5. **完了**: リモート画面が表示されれば成功！

---

**準備ができたら、クライアントを起動して接続してみてください！**



