# ステップ5以降の詳細ガイド

## 📍 ステップ5: デスクトップ側のIPアドレスを確認

### デスクトップ側で実行すること

#### 手順1: デスクトップ側のPCでコマンドプロンプトを開く

1. **Windowsキー + R** を同時に押す
2. 「**cmd**」と入力
3. **Enterキー**を押す

#### 手順2: ipconfigコマンドを実行

コマンドプロンプトに以下を入力してEnterキーを押す:

```bash
ipconfig
```

#### 手順3: IPv4アドレスを確認

表示される内容の例:

```
Windows IP構成

イーサネット アダプター イーサネット:

   接続固有の DNS サフィックス . . . :
   リンクローカル IPv6 アドレス . . . : fe80::xxxx:xxxx:xxxx:xxxx%xx
   IPv4 アドレス . . . . . . . . . . . : 192.168.1.100    ← これ！
   サブネット マスク . . . . . . . . . : 255.255.255.0
   デフォルト ゲートウェイ . . . . . . : 192.168.1.1
```

**「IPv4 アドレス」の右側に表示されている数字をメモしてください。**

例: `192.168.1.100`

**このIPアドレスをノートパソコン側で使用します。**

---

## 📍 ステップ6: クライアントを起動

### ノートパソコン側で実行すること

#### 方法A: バッチファイルから起動（最も簡単）

##### 手順1: エクスプローラーでフォルダを開く

1. ノートパソコンのデスクトップで、作成したフォルダを**ダブルクリック**
2. フォルダが開きます

##### 手順2: バッチファイルを実行

1. フォルダ内の `start_custom_remote_client.bat` を**ダブルクリック**
2. コマンドプロンプトウィンドウが開きます

##### 手順3: IPアドレスを入力

コマンドプロンプトに以下のような表示が出ます:

```
========================================
カスタムリモートデスクトップ - クライアント
ノートパソコン側で実行してください
========================================

サーバーIPアドレスを入力してください (デフォルト: 192.168.1.100): 
```

**ここで、ステップ5で確認したIPアドレスを入力します。**

例: `192.168.1.100` と入力してEnterキーを押す

##### 手順4: 接続開始

入力すると、接続が開始されます。

---

#### 方法B: コマンドプロンプトから起動

##### 手順1: コマンドプロンプトを開く

1. エクスプローラーでデスクトップのフォルダを開く
2. アドレスバーに「**cmd**」と入力してEnterキーを押す

##### 手順2: クライアントを起動

コマンドプロンプトに以下を入力してEnterキーを押す:

```bash
python custom_remote_desktop.py client 192.168.1.100
```

（`192.168.1.100` の部分を、ステップ5で確認したIPアドレスに変更）

**例:**
```bash
python custom_remote_desktop.py client 192.168.1.100
```

##### 手順3: 接続開始

コマンドを実行すると、接続が開始されます。

---

## 📍 ステップ7: 接続成功の確認

### 接続が成功した場合

#### 画面の表示

1. **GUIウィンドウが開きます**
   - タイトル: 「リモートデスクトップ - クライアント」
   - サイズ: 1024x768（デフォルト）

2. **接続状態の表示**
   - 上部に「接続中: 192.168.1.100:8888」と表示
   - その後、「接続成功: 192.168.1.100」と表示

3. **デスクトップの画面が表示されます**
   - ウィンドウ内に、デスクトップ側の画面が表示されます
   - リアルタイムで更新されます

#### 操作方法

- **マウス操作**: 
  - ウィンドウ内でマウスを動かすと、デスクトップ側のマウスが動きます
  - 画面をクリックすると、デスクトップ側でクリックされます

- **キーボード操作**: 
  - キーを押すと、デスクトップ側で入力されます

- **右クリック**: 
  - 画面を右クリックすると、デスクトップ側で右クリックされます

#### 切断方法

- ウィンドウの「切断」ボタンをクリック
- または、ウィンドウを閉じる

---

## 🔴 接続できない場合の対処法

### エラー1: 「サーバーに接続できませんでした」

#### 原因
- デスクトップ側でサーバーが起動していない
- IPアドレスが間違っている
- ファイアウォールがブロックしている

#### 対処法

1. **デスクトップ側でサーバーが起動しているか確認**
   - デスクトップ側のコマンドプロンプトに「[サーバー] 待機中: 0.0.0.0:8888」と表示されているか確認

2. **IPアドレスが正しいか確認**
   - デスクトップ側で再度 `ipconfig` を実行して確認

3. **ファイアウォールの設定を確認**
   - Windowsファイアウォールでポート8888がブロックされていないか確認

### エラー2: 「画面が表示されない」

#### 原因
- ライブラリがインストールされていない
- ネットワーク接続が遅い

#### 対処法

1. **ライブラリがインストールされているか確認**
   ```bash
   python -m pip list | findstr mss
   python -m pip list | findstr pyautogui
   ```

2. **ライブラリを再インストール**
   ```bash
   python -m pip install mss pillow pyautogui
   ```

---

## 📋 実際の操作例

### 例: IPアドレスが `192.168.1.100` の場合

#### デスクトップ側（サーバー）

1. コマンドプロンプトを開く
2. サーバーを起動:
   ```bash
   python custom_remote_desktop.py server
   ```
3. 表示:
   ```
   === リモートデスクトップサーバー ===
   デスクトップ側で実行してください
   クライアントの接続を待機しています...
   [サーバー] 待機中: 0.0.0.0:8888
   ```
4. この状態で待機

#### ノートパソコン側（クライアント）

1. コマンドプロンプトを開く
2. クライアントを起動:
   ```bash
   python custom_remote_desktop.py client 192.168.1.100
   ```
3. GUIウィンドウが開く
4. デスクトップの画面が表示される

---

## 💡 ヒント

### デスクトップ側のIPアドレスを簡単に確認する方法

デスクトップ側で、以下のコマンドを実行すると、IPv4アドレスのみが表示されます:

```bash
ipconfig | findstr IPv4
```

**表示例:**
```
   IPv4 アドレス . . . . . . . . . . . : 192.168.1.100
```

### 接続状態を確認する方法

ノートパソコン側のGUIウィンドウの上部に、接続状態が表示されます:

- **「接続中: 192.168.1.100:8888」** → 接続中
- **「接続成功: 192.168.1.100」** → 接続成功
- **「接続失敗: 192.168.1.100」** → 接続失敗

---

## 📝 まとめ

### ステップ5: IPアドレス確認
1. デスクトップ側で `ipconfig` を実行
2. IPv4アドレスをメモ（例: `192.168.1.100`）

### ステップ6: クライアント起動
1. ノートパソコン側でコマンドプロンプトを開く
2. `python custom_remote_desktop.py client 192.168.1.100` を実行
   （IPアドレスを実際の値に変更）

### ステップ7: 接続確認
1. GUIウィンドウが開く
2. デスクトップの画面が表示される
3. マウスとキーボードで操作できる

これで、リモートデスクトップ接続が完了です！





