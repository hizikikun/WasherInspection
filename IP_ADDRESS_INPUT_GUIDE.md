# IPアドレス入力ガイド

## 📍 現在の状態

画面に以下のように表示されているはずです:

```
サーバー IPアドレスを入力してください(デフォルト: 192.168.1.100): 
```

これは、デスクトップ側のIPアドレスを入力する待ち状態です。

---

## 🎯 次のステップ

### ステップ1: デスクトップ側のIPアドレスを確認

**デスクトップ側のPCで**、以下の操作を行ってください:

1. **コマンドプロンプトを開く**
   - Windowsキー + R → 「cmd」と入力 → Enter

2. **ipconfigコマンドを実行**
   ```bash
   ipconfig
   ```

3. **IPv4アドレスを確認**
   表示される内容の例:
   ```
   IPv4 アドレス . . . . . . . . . . . : 192.168.1.100
   ```
   
   **この数字をメモしてください。**

### ステップ2: IPアドレスを入力

ノートパソコンの画面に戻り、IPアドレスを入力します。

#### 入力方法

1. **IPアドレスを入力**
   - 例: `192.168.1.100` と入力
   - デスクトップ側で確認したIPアドレスを入力してください

2. **Enterキーを押す**
   - 入力が完了したら、Enterキーを押します

#### 入力例

画面に以下のように表示されている場合:

```
サーバー IPアドレスを入力してください(デフォルト: 192.168.1.100): 
```

**デスクトップ側のIPアドレスが `192.168.1.100` の場合:**

1. `192.168.1.100` と入力
2. Enterキーを押す

**デフォルト値を使う場合:**

1. 何も入力せずにEnterキーを押す
   - デフォルト値（`192.168.1.100`）が使用されます

---

## 💡 よくある質問

### Q: IPアドレスがわからない場合は？

A: デスクトップ側のPCで `ipconfig` を実行して確認してください。

### Q: デフォルト値を使ってもいいですか？

A: デスクトップ側のIPアドレスがデフォルト値（`192.168.1.100`）と同じ場合は、何も入力せずにEnterキーを押すだけでOKです。

### Q: 入力ミスをした場合は？

A: コマンドプロンプトを閉じて、もう一度 `start_custom_remote_client.bat` を実行してください。

### Q: 接続できない場合は？

A: 以下の点を確認してください:
1. デスクトップ側でサーバーが起動しているか
2. IPアドレスが正しいか
3. 両方のPCが同じネットワークに接続されているか

---

## 📋 入力後の動作

IPアドレスを入力してEnterキーを押すと:

1. **接続が開始されます**
   - 画面に接続中のメッセージが表示されます

2. **GUIウィンドウが開きます**
   - リモートデスクトップの画面が表示されます

3. **接続成功**
   - デスクトップ側の画面が表示されれば成功です

---

## 🔴 エラーが出た場合

### エラー: 「サーバーに接続できませんでした」

#### 原因と対処法

1. **デスクトップ側でサーバーが起動していない**
   - デスクトップ側で `python custom_remote_desktop.py server` を実行してください

2. **IPアドレスが間違っている**
   - デスクトップ側で再度 `ipconfig` を実行して確認してください

3. **ファイアウォールがブロックしている**
   - Windowsファイアウォールでポート8888がブロックされていないか確認してください

---

## 📝 まとめ

1. ✅ 画面に「サーバー IPアドレスを入力してください」と表示されている
2. ✅ デスクトップ側で `ipconfig` を実行してIPアドレスを確認
3. ✅ ノートパソコンの画面でIPアドレスを入力（例: `192.168.1.100`）
4. ✅ Enterキーを押す
5. ✅ GUIウィンドウが開き、デスクトップの画面が表示される

これで接続完了です！





