# デスクトップ側のフォルダ作成（お勧めの方法）

## 📁 お勧めの方法

デスクトップ画面にフォルダを作成する必要はありません。以下の方法がお勧めです。

---

## 🎯 方法1: WasherInspectionフォルダ内に作成（最も簡単・推奨）

### メリット
- 既存のフォルダを活用できる
- ファイルをコピーする必要がない
- 管理が簡単

### 手順

1. **エクスプローラーを開く**
   - **Windowsキー + E** を同時に押す

2. **WasherInspectionフォルダを開く**
   - 左側のサイドバーで「PC」→「ローカルディスク(C:)」→「Users」→「西村康成」→「WasherInspection」をクリック
   - または、アドレスバーに以下を入力してEnter:
     ```
     C:\Users\西村康成\WasherInspection
     ```

3. **ファイルが存在するか確認**
   - `custom_remote_desktop.py` が存在するか確認
   - `start_custom_remote_server.bat` が存在するか確認

4. **これで完了！**
   - ファイルをコピーする必要はありません
   - このフォルダから直接サーバーを起動できます

---

## 🎯 方法2: ユーザーフォルダ直下に作成

### メリット
- アクセスが簡単
- パスが短い

### 手順

1. **エクスプローラーを開く**
   - **Windowsキー + E** を同時に押す

2. **ユーザーフォルダを開く**
   - 左側のサイドバーで「PC」→「ローカルディスク(C:)」→「Users」→「西村康成」をクリック
   - または、アドレスバーに以下を入力してEnter:
     ```
     C:\Users\西村康成
     ```

3. **新しいフォルダを作成**
   - フォルダ内の空白部分を**右クリック**
   - 「**新規作成**」→「**フォルダ**」を選択
   - フォルダ名を入力（例: `RemoteDesktop`）
   - **Enterキー**を押す

4. **ファイルをコピー**
   - `WasherInspection` フォルダから `custom_remote_desktop.py` と `start_custom_remote_server.bat` をコピー
   - 作成した `RemoteDesktop` フォルダに貼り付け

---

## 🎯 方法3: ドキュメントフォルダに作成

### メリット
- ドキュメントとして管理できる
- バックアップしやすい

### 手順

1. **エクスプローラーを開く**
   - **Windowsキー + E** を同時に押す

2. **ドキュメントフォルダを開く**
   - 左側のサイドバーで「**ドキュメント**」をクリック
   - または、アドレスバーに以下を入力してEnter:
     ```
     C:\Users\西村康成\Documents
     ```

3. **新しいフォルダを作成**
   - フォルダ内の空白部分を**右クリック**
   - 「**新規作成**」→「**フォルダ**」を選択
   - フォルダ名を入力（例: `RemoteDesktop`）
   - **Enterキー**を押す

4. **ファイルをコピー**
   - `WasherInspection` フォルダから `custom_remote_desktop.py` と `start_custom_remote_server.bat` をコピー
   - 作成した `RemoteDesktop` フォルダに貼り付け

---

## 💡 最も簡単な方法（推奨）

### WasherInspectionフォルダをそのまま使用

ファイルをコピーする必要がなく、最も簡単です。

#### 手順

1. **エクスプローラーを開く**
   - **Windowsキー + E** を同時に押す

2. **WasherInspectionフォルダを開く**
   - アドレスバーに以下を入力してEnter:
     ```
     C:\Users\西村康成\WasherInspection
     ```

3. **コマンドプロンプトを開く**
   - アドレスバーに「**cmd**」と入力してEnterキーを押す

4. **サーバーを起動**
   - コマンドプロンプトに以下を入力してEnter:
     ```bash
     python custom_remote_desktop.py server
     ```

**これで完了！** ファイルをコピーする必要はありません。

---

## 📋 各方法の比較

| 方法 | メリット | デメリット |
|------|---------|-----------|
| **WasherInspectionフォルダ内** | ファイルコピー不要、最も簡単 | 既存のフォルダが混在する |
| **ユーザーフォルダ直下** | アクセスが簡単、パスが短い | ファイルをコピーする必要がある |
| **ドキュメントフォルダ** | 管理しやすい、バックアップしやすい | ファイルをコピーする必要がある |

---

## 🚀 推奨手順（WasherInspectionフォルダを使用）

### ステップ1: エクスプローラーでフォルダを開く

1. **Windowsキー + E** を同時に押す
2. アドレスバーに以下を入力してEnter:
   ```
   C:\Users\西村康成\WasherInspection
   ```

### ステップ2: コマンドプロンプトを開く

1. アドレスバーに「**cmd**」と入力してEnterキーを押す
2. コマンドプロンプトが開きます
3. パスが `C:\Users\西村康成\WasherInspection>` のように表示されます

### ステップ3: ライブラリをインストール

コマンドプロンプトに以下を入力してEnter:

```bash
python -m pip install mss pillow pyautogui
```

### ステップ4: IPアドレスを確認

コマンドプロンプトに以下を入力してEnter:

```bash
ipconfig
```

IPv4アドレスをメモ（例: `192.168.1.100`）

### ステップ5: サーバーを起動

コマンドプロンプトに以下を入力してEnter:

```bash
python custom_remote_desktop.py server
```

### ステップ6: 接続を待機

「[サーバー] 待機中: 0.0.0.0:8888」と表示されればOKです。

---

## 📝 まとめ

**最も簡単な方法:**

1. ✅ **WasherInspectionフォルダを開く**
   - アドレスバーに `C:\Users\西村康成\WasherInspection` と入力

2. ✅ **コマンドプロンプトを開く**
   - アドレスバーに「cmd」と入力してEnter

3. ✅ **サーバーを起動**
   - `python custom_remote_desktop.py server` を実行

**ファイルをコピーする必要はありません！**

これが最も簡単でお勧めの方法です。





