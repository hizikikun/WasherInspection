# リモートデスクトップ管理アプリ

リモートデスクトップ接続を簡単に管理・操作できるGUIアプリケーションです。

## 🎯 主な機能

- ✅ **複数の接続先を管理**: Windows RDP、TeamViewer、AnyDesk、Chrome Remote Desktop、VNCに対応
- ✅ **ワンクリック接続**: 保存した接続先にワンクリックで接続
- ✅ **接続履歴の記録**: 接続の成功/失敗を自動記録
- ✅ **接続状態の確認**: 接続先の状態を確認
- ✅ **設定の自動保存**: 接続情報を自動的に保存

## 🚀 使い方

### 起動方法

1. **バッチファイルから起動（推奨）**
   ```bash
   start_remote_desktop_manager.bat
   ```

2. **Pythonから直接起動**
   ```bash
   python remote_desktop_manager.py
   ```

### 基本的な使い方

1. **接続の追加**
   - 「追加」ボタンをクリック
   - 接続情報を入力：
     - **名前**: 接続先の名前（例: "自宅デスクトップ"）
     - **タイプ**: 接続タイプを選択
     - **アドレス/ID**: IPアドレスまたはID
     - **ユーザー名**: 必要に応じて入力
     - **メモ**: 任意のメモを入力
   - 「OK」をクリック

2. **接続**
   - 接続一覧から接続先を選択
   - 「接続」ボタンをクリック、または接続先をダブルクリック

3. **接続の編集・削除**
   - 接続先を選択して「編集」または「削除」ボタンをクリック

4. **接続状態の確認**
   - 接続先を選択して「接続状態を確認」ボタンをクリック

## 📋 対応している接続タイプ

### Windows RDP
- **アドレス**: IPアドレスまたはコンピューター名（例: `192.168.1.100`）
- **ユーザー名**: リモートデスクトップのユーザー名
- **注意**: 大学のWiFiからは接続できない可能性があります

### TeamViewer
- **アドレス**: TeamViewer ID（例: `123456789`）
- **必要**: TeamViewerがインストールされている必要があります
- **推奨**: 大学のWiFiからでも接続可能

### AnyDesk
- **アドレス**: AnyDeskアドレス（例: `123456789`）
- **必要**: AnyDeskがインストールされている必要があります
- **推奨**: 大学のWiFiからでも接続可能

### Chrome Remote Desktop
- **アドレス**: リモートアクセスID
- **必要**: ChromeブラウザとChrome Remote Desktop拡張機能が必要です
- **推奨**: 大学のWiFiからでも接続可能

### VNC
- **アドレス**: IPアドレスまたはホスト名（例: `192.168.1.100`）
- **ユーザー名**: 必要に応じて入力
- **必要**: VNCビューアーがインストールされている必要があります

## 💾 データの保存場所

- **接続設定**: `remote_desktop_config.json`
- **接続履歴**: `remote_desktop_history.json`

これらのファイルは、アプリと同じディレクトリに保存されます。

## 🔧 トラブルシューティング

### 接続できない場合

1. **ソフトウェアがインストールされているか確認**
   - TeamViewer、AnyDesk、VNCビューアーなど、必要なソフトウェアがインストールされているか確認してください

2. **アドレス/IDが正しいか確認**
   - 接続先のアドレスやIDが正しく入力されているか確認してください

3. **ネットワーク接続を確認**
   - 両方のPCがインターネットに接続されているか確認してください

4. **ファイアウォールの設定を確認**
   - ファイアウォールが接続をブロックしていないか確認してください

### アプリが起動しない場合

1. **Pythonがインストールされているか確認**
   ```bash
   python --version
   ```

2. **必要なライブラリがインストールされているか確認**
   - このアプリは標準ライブラリのみを使用しているため、追加のインストールは不要です

## 📝 使用例

### 例1: 自宅のデスクトップにTeamViewerで接続

1. 「追加」ボタンをクリック
2. 以下の情報を入力：
   - 名前: `自宅デスクトップ`
   - タイプ: `TeamViewer`
   - アドレス/ID: `123456789`（TeamViewer ID）
3. 「OK」をクリック
4. 接続先を選択して「接続」ボタンをクリック

### 例2: 大学のWiFiからWindows RDPで接続（ポートフォワーディング設定済み）

1. 「追加」ボタンをクリック
2. 以下の情報を入力：
   - 名前: `自宅デスクトップ（RDP）`
   - タイプ: `Windows RDP`
   - アドレス/ID: `your-dynamic-dns.example.com`（またはIPアドレス）
   - ユーザー名: `your-username`
3. 「OK」をクリック
4. 接続先を選択して「接続」ボタンをクリック

## 🔒 セキュリティについて

- **パスワードの保存**: 現在のバージョンでは、パスワードは暗号化せずに保存されます。セキュリティが重要な場合は、パスワードを保存しないことをお勧めします。
- **設定ファイル**: `remote_desktop_config.json`には接続情報が保存されます。このファイルを共有する場合は注意してください。

## 🎨 スクリーンショット

アプリの主な画面：

- **左側**: 接続一覧（追加、編集、削除、接続ボタン）
- **右側**: 接続詳細と接続履歴

## 📚 関連ドキュメント

- `REMOTE_DESKTOP_SETUP_GUIDE.md`: リモートデスクトップの設定方法
- `setup_remote_desktop.ps1`: Windows RDPの有効化スクリプト

## 🆘 サポート

問題が発生した場合は、以下を確認してください：

1. エラーメッセージを確認
2. 接続履歴で失敗の原因を確認
3. `REMOTE_DESKTOP_SETUP_GUIDE.md`を参照

---

**注意**: 大学のWiFiから接続する場合は、TeamViewerまたはAnyDeskの使用を強く推奨します。Windows RDPはポート3389がブロックされている可能性が高いです。







