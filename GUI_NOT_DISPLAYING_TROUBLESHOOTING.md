# GUIが表示されない場合のトラブルシューティング

## 🔴 問題の症状

GUIがダウンロードしているが表示されず、原因もわからない。

---

## 🔍 原因の確認

GUIが表示されない場合、以下のいずれかが原因です:

1. **tkinterがインストールされていない**
2. **GUIウィンドウが開いているが、タスクバーに隠れている**
3. **エラーが発生しているが表示されていない**
4. **ウィンドウが画面外に表示されている**
5. **接続は成功しているが、画面が表示されない**

---

## ✅ 解決方法

### ステップ1: エラーメッセージを確認

#### コマンドプロンプトから直接実行

バッチファイルではなく、コマンドプロンプトから直接実行してエラーメッセージを確認してください。

1. **コマンドプロンプトを開く**
   - エクスプローラーで `RemoteDesktop` フォルダを開く
   - アドレスバーに「cmd」と入力してEnter

2. **クライアントを起動**
   ```bash
   python custom_remote_desktop.py client 192.168.1.100
   ```
   （`192.168.1.100` を実際のIPアドレスに変更）

3. **エラーメッセージを確認**
   - コマンドプロンプトにエラーメッセージが表示されます
   - エラーメッセージの内容をメモしてください

---

### ステップ2: tkinterがインストールされているか確認

#### 確認方法

コマンドプロンプトに以下を入力してEnter:

```bash
python -m tkinter
```

**結果:**

- **ウィンドウが開く場合**: tkinterはインストールされています ✅
- **エラーが表示される場合**: tkinterがインストールされていません ❌

#### tkinterがインストールされていない場合

1. **Pythonを再インストール**
   - https://www.python.org/downloads/ からダウンロード
   - インストール時に「**tcl/tk and IDLE**」にチェックを入れる
   - インストール時に「**Add Python to PATH**」にもチェックを入れる

2. **インストール後、コマンドプロンプトを再起動**

3. **再度確認**
   ```bash
   python -m tkinter
   ```

---

### ステップ3: タスクバーでウィンドウを確認

#### 確認方法

1. **タスクバーを確認**
   - 画面下部のタスクバーを確認
   - 「リモートデスクトップ」や「Python」などのアイコンが表示されていないか確認

2. **Alt + Tabでウィンドウを確認**
   - **Alt + Tab** を同時に押す
   - 開いているウィンドウの一覧が表示されます
   - 「リモートデスクトップ」や「Python」のウィンドウがないか確認

3. **ウィンドウが見つかった場合**
   - ウィンドウをクリックして前面に表示

---

### ステップ4: ウィンドウが画面外に表示されている場合

#### 確認方法

1. **Alt + Tabでウィンドウを選択**
   - **Alt + Tab** を押してウィンドウを選択

2. **ウィンドウを前面に表示**
   - **Alt + スペース** を押す
   - 「移動」を選択
   - 矢印キーでウィンドウを移動

3. **または、ウィンドウを最大化**
   - **Alt + スペース** を押す
   - 「最大化」を選択

---

### ステップ5: 接続状態を確認

#### コマンドプロンプトで確認

コマンドプロンプトに以下が表示されているか確認:

- **「接続中: 192.168.1.100:8888」** → 接続を試みています
- **「接続成功: 192.168.1.100」** → 接続成功
- **「接続失敗: 192.168.1.100」** → 接続失敗
- **エラーメッセージ** → エラーが発生しています

---

## 🔴 よくあるエラーと対処法

### エラー1: 「ModuleNotFoundError: No module named 'tkinter'」

#### 原因
tkinterがインストールされていない

#### 対処法

1. **Pythonを再インストール**
   - https://www.python.org/downloads/ からダウンロード
   - インストール時に「**tcl/tk and IDLE**」にチェックを入れる
   - インストール時に「**Add Python to PATH**」にもチェックを入れる

2. **インストール後、コマンドプロンプトを再起動**

3. **再度実行**
   ```bash
   python custom_remote_desktop.py client 192.168.1.100
   ```

---

### エラー2: 「ModuleNotFoundError: No module named 'mss'」

#### 原因
mssライブラリがインストールされていない

#### 対処法
```bash
python -m pip install mss pillow pyautogui
```

インストール後、再度実行:
```bash
python custom_remote_desktop.py client 192.168.1.100
```

---

### エラー3: 「サーバーに接続できませんでした」

#### 原因
- デスクトップ側でサーバーが起動していない
- IPアドレスが間違っている

#### 対処法

1. **デスクトップ側でサーバーが起動しているか確認**
   - デスクトップ側のコマンドプロンプトに「[サーバー] 待機中: 0.0.0.0:8888」と表示されているか確認

2. **IPアドレスが正しいか確認**
   - デスクトップ側で再度 `ipconfig` を実行して確認

---

## 🔧 デバッグ方法

### 方法1: 詳細なエラーメッセージを表示

コマンドプロンプトから直接実行して、エラーメッセージを確認:

```bash
python -u custom_remote_desktop.py client 192.168.1.100
```

### 方法2: Pythonの対話モードで確認

1. **Pythonの対話モードに入る**
   ```bash
   python
   ```

2. **tkinterをインポート**
   ```python
   import tkinter
   ```

3. **エラーが表示される場合**
   - tkinterがインストールされていません
   - Pythonを再インストールしてください

4. **エラーが表示されない場合**
   - tkinterはインストールされています
   - 他の原因を確認してください

5. **対話モードを終了**
   ```python
   exit()
   ```

---

## 📋 チェックリスト

- [ ] コマンドプロンプトから直接実行してエラーメッセージを確認
- [ ] tkinterがインストールされているか確認（`python -m tkinter`）
- [ ] タスクバーでウィンドウを確認
- [ ] Alt + Tabでウィンドウを確認
- [ ] 接続状態を確認（コマンドプロンプトの表示）
- [ ] 必要なライブラリがインストールされているか確認

---

## 💡 簡単な確認方法

### ステップ1: tkinterの確認

```bash
python -m tkinter
```

**ウィンドウが開けば、tkinterは利用可能です。**

### ステップ2: コマンドプロンプトから直接実行

```bash
python custom_remote_desktop.py client 192.168.1.100
```

**エラーメッセージが表示されれば、内容を確認してください。**

### ステップ3: タスクバーとAlt + Tabで確認

- タスクバーにウィンドウが表示されていないか確認
- Alt + Tabでウィンドウの一覧を確認

---

## 🆘 それでも解決しない場合

### デバッグ情報を収集

以下の情報をメモしてください:

1. **実行したコマンド**
   - コマンドプロンプトに入力したコマンド

2. **エラーメッセージ（あれば）**
   - コマンドプロンプトに表示されるエラーメッセージの全文

3. **tkinterの確認結果**
   - `python -m tkinter` の結果

4. **接続状態**
   - コマンドプロンプトに表示される接続状態

5. **タスクバーとAlt + Tabの確認結果**
   - ウィンドウが表示されているか

これらの情報があれば、さらに詳しくサポートできます。

---

## 📝 まとめ

1. ✅ **コマンドプロンプトから直接実行**してエラーメッセージを確認
2. ✅ **tkinterがインストールされているか確認**（`python -m tkinter`）
3. ✅ **タスクバーとAlt + Tabでウィンドウを確認**
4. ✅ **接続状態を確認**（コマンドプロンプトの表示）

**エラーメッセージや表示されている内容を教えていただければ、さらに詳しくサポートできます。**





