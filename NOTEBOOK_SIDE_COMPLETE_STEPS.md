# ノートパソコン側の完全な操作手順

## 📋 ノートパソコン側で必要な操作（最初から最後まで）

---

## 💻 ステップ1: フォルダを作成

### 手順

1. **エクスプローラーを開く**
   - **Windowsキー + E** を同時に押す
   - または、タスクバーのフォルダアイコンをクリック

2. **デスクトップに移動**
   - 左側のサイドバーで「**デスクトップ**」をクリック

3. **新しいフォルダを作成**
   - デスクトップの空白部分を**右クリック**
   - 「**新規作成**」→「**フォルダ**」を選択
   - フォルダ名を入力（例: `RemoteDesktop`）
   - **Enterキー**を押す

---

## 📁 ステップ2: 必要なファイルをコピー

### 手順

1. **デスクトップ側からファイルを取得**
   - USBメモリ、クラウドストレージ、メールなどでファイルを転送
   - または、デスクトップ側のPCから直接コピー

2. **ファイルをコピー**
   - `custom_remote_desktop.py` をコピー
   - `start_custom_remote_client.bat` をコピー

3. **作成したフォルダに貼り付け**
   - ノートパソコンのデスクトップの `RemoteDesktop` フォルダを**ダブルクリック**して開く
   - フォルダ内の空白部分を**右クリック** → 「**貼り付け**」

4. **ファイルがコピーされたか確認**
   - フォルダ内に以下のファイルがあるか確認:
     - ✅ `custom_remote_desktop.py`
     - ✅ `start_custom_remote_client.bat`

---

## 💻 ステップ3: コマンドプロンプトを開く

### 方法A: エクスプローラーから開く（簡単・推奨）

1. **エクスプローラーでフォルダを開く**
   - デスクトップの `RemoteDesktop` フォルダを**ダブルクリック**して開く

2. **アドレスバーに「cmd」と入力**
   - フォルダを開いた状態で、上部の**アドレスバー**（パスが表示されている場所）を**クリック**
   - 「**cmd**」と入力して**Enterキー**を押す

3. **コマンドプロンプトが開く**
   - そのフォルダでコマンドプロンプトが開きます
   - パスが `C:\Users\kouse\Desktop\RemoteDesktop>` のように表示されます

### 方法B: Windowsキー + Rから開く

1. **Windowsキー + R** を同時に押す
2. 「**cmd**」と入力して**Enterキー**を押す
3. コマンドプロンプトが開きます
4. フォルダに移動:
   ```bash
   cd "C:\Users\kouse\Desktop\RemoteDesktop"
   ```
   - **Enterキー**を押す
   - （フォルダの場所に合わせて変更してください）

---

## 🐍 ステップ4: Pythonがインストールされているか確認

### 手順

1. **コマンドプロンプトに以下を入力**
   ```bash
   python --version
   ```

2. **Enterキーを押す**

3. **Pythonのバージョンが表示されるか確認**
   - 表示例:
     ```
     Python 3.11.5
     ```

4. **Pythonが表示されない場合**
   - Pythonをインストールする必要があります
   - https://www.python.org/downloads/ からダウンロード
   - インストール時に「**Add Python to PATH**」にチェックを入れる
   - インストール後、コマンドプロンプトを再起動

---

## 📦 ステップ5: ライブラリをインストール

### 手順

1. **コマンドプロンプトに以下を入力**
   ```bash
   python -m pip install mss pillow pyautogui
   ```

2. **Enterキーを押す**

3. **インストールが完了するまで待つ**
   - 画面に以下のように表示されます:
     ```
     Collecting mss
       Downloading mss-9.0.1-py3-none-any.whl
     ...
     Successfully installed mss-9.0.1 pillow-10.1.0 pyautogui-0.9.54
     ```

4. **インストールが完了したら、次のステップに進む**

---

## 🔍 ステップ6: デスクトップ側のIPアドレスを確認

### 手順

1. **デスクトップ側のPCでIPアドレスを確認**
   - デスクトップ側でコマンドプロンプトを開く
   - `ipconfig` を実行
   - IPv4アドレスをメモ（例: `192.168.1.100`）

2. **ノートパソコン側でIPアドレスをメモ**
   - デスクトップ側で確認したIPアドレスをメモしてください
   - 例: `192.168.1.100`

---

## 🚀 ステップ7: クライアントを起動

### 方法A: バッチファイルから起動（簡単）

1. **エクスプローラーでフォルダを開く**
   - デスクトップの `RemoteDesktop` フォルダを開く

2. **バッチファイルを実行**
   - `start_custom_remote_client.bat` を**ダブルクリック**

3. **IPアドレスを入力**
   - コマンドプロンプトに以下のように表示されます:
     ```
     サーバーIPアドレスを入力してください (デフォルト: 192.168.1.100): 
     ```
   - デスクトップ側で確認したIPアドレスを入力（例: `192.168.1.100`）
   - **Enterキー**を押す

4. **「Press any key to continue...」と表示されたら**
   - 何かキーを押す（Enterキーなど）
   - コマンドプロンプトウィンドウが閉じます

5. **GUIウィンドウが開く**
   - リモートデスクトップのGUIウィンドウが開きます
   - デスクトップ側の画面が表示されます

### 方法B: コマンドプロンプトから起動

1. **コマンドプロンプトに以下を入力**
   ```bash
   python custom_remote_desktop.py client 192.168.1.100
   ```
   （`192.168.1.100` の部分を、デスクトップ側で確認したIPアドレスに変更）

2. **Enterキーを押す**

3. **GUIウィンドウが開く**
   - リモートデスクトップのGUIウィンドウが開きます
   - デスクトップ側の画面が表示されます

---

## ✅ ステップ8: 接続成功の確認

### 正常に接続できた場合

1. **GUIウィンドウが開く**
   - タイトル: 「リモートデスクトップ - クライアント」
   - サイズ: 1024x768（デフォルト）

2. **接続状態の表示**
   - 上部に「接続中: 192.168.1.100:8888」と表示
   - その後、「接続成功: 192.168.1.100」と表示

3. **デスクトップの画面が表示される**
   - ウィンドウ内に、デスクトップ側の画面が表示されます
   - リアルタイムで更新されます

### 操作方法

- **マウス操作**: ウィンドウ内でマウスを動かすと、デスクトップ側のマウスが動きます
- **クリック**: 画面をクリックすると、デスクトップ側でクリックされます
- **キーボード**: キーを押すと、デスクトップ側で入力されます
- **右クリック**: 画面を右クリックすると、デスクトップ側で右クリックされます

### 切断方法

- ウィンドウの「切断」ボタンをクリック
- または、ウィンドウを閉じる

---

## 📋 ノートパソコン側のチェックリスト

- [ ] フォルダを作成した（`RemoteDesktop`）
- [ ] `custom_remote_desktop.py` をコピーした
- [ ] `start_custom_remote_client.bat` をコピーした
- [ ] コマンドプロンプトを開いた
- [ ] Pythonがインストールされている（`python --version`）
- [ ] ライブラリをインストールした（`python -m pip install mss pillow pyautogui`）
- [ ] デスクトップ側のIPアドレスを確認した
- [ ] クライアントを起動した（`python custom_remote_desktop.py client [IPアドレス]`）
- [ ] GUIウィンドウが開いた
- [ ] デスクトップの画面が表示された

---

## 🔴 エラーが発生した場合

### エラー1: 「'python' は、内部コマンドまたは外部コマンド...」

**対処法:**
1. Pythonをインストール: https://www.python.org/downloads/
2. インストール時に「**Add Python to PATH**」にチェックを入れる
3. コマンドプロンプトを再起動

### エラー2: 「ModuleNotFoundError: No module named 'mss'」

**対処法:**
```bash
python -m pip install mss pillow pyautogui
```

### エラー3: 「サーバーに接続できませんでした」

**対処法:**
1. デスクトップ側でサーバーが起動しているか確認
2. IPアドレスが正しいか確認
3. 両方のPCが同じネットワークに接続されているか確認

---

## 💡 ヒント

### デスクトップ側のIPアドレスを確認する方法

デスクトップ側で、以下のコマンドを実行すると、IPv4アドレスのみが表示されます:

```bash
ipconfig | findstr IPv4
```

**表示例:**
```
   IPv4 アドレス . . . . . . . . . . . : 192.168.1.100
```

### 接続状態を確認する方法

ノートパソコン側のGUIウィンドウの上部に、接続状態が表示されます:

- **「接続中: 192.168.1.100:8888」** → 接続中
- **「接続成功: 192.168.1.100」** → 接続成功
- **「接続失敗: 192.168.1.100」** → 接続失敗

---

## 📝 まとめ

ノートパソコン側で必要な操作:

1. ✅ フォルダを作成
2. ✅ ファイルをコピー
3. ✅ コマンドプロンプトを開く
4. ✅ Pythonがインストールされているか確認
5. ✅ ライブラリをインストール
6. ✅ デスクトップ側のIPアドレスを確認
7. ✅ クライアントを起動
8. ✅ 接続成功の確認

**これで、ノートパソコン側の準備は完了です！**





