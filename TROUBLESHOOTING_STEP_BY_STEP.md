# ステップバイステップ トラブルシューティング

## 🔍 現在の状態を確認

「Python」とだけ表示されて結果が変わらない場合、以下の手順で確認してください。

---

## 📋 ステップ1: コマンドプロンプトの状態を確認

### 現在の表示を確認

コマンドプロンプトに何が表示されているか確認してください:

- **`>>>` が表示されている場合**: Pythonの対話モードに入っています
- **`C:\Users\kouse\...>` が表示されている場合**: コマンドプロンプトの通常モードです
- **「Python」とだけ表示されている場合**: 何か別の問題があります

---

## 🎯 ステップ2: コマンドプロンプトを再起動

### 方法1: 新しいコマンドプロンプトを開く

1. **現在のコマンドプロンプトを閉じる**
   - ウィンドウを閉じる（×ボタンをクリック）

2. **エクスプローラーでフォルダを開く**
   - エクスプローラーを開く（Windowsキー + E）
   - `C:\Users\kouse\OneDrive\ドキュメント\デスクトップ\Remote Desktop` フォルダを開く

3. **アドレスバーに「cmd」と入力**
   - フォルダを開いた状態で、上部のアドレスバーをクリック
   - 「**cmd**」と入力してEnterキーを押す

4. **新しいコマンドプロンプトが開く**
   - パスが `C:\Users\kouse\OneDrive\ドキュメント\デスクトップ\Remote Desktop>` のように表示されます

---

## 📁 ステップ3: ファイルが存在するか確認

### ファイルの存在確認

コマンドプロンプトに以下を入力してEnterキーを押す:

```bash
dir
```

**表示される内容の例:**
```
 Volume in drive C has no label.
 Volume Serial Number is XXXX-XXXX

 Directory of C:\Users\kouse\OneDrive\ドキュメント\デスクトップ\Remote Desktop

2024/01/01  10:00    <DIR>          .
2024/01/01  10:00    <DIR>          ..
2024/01/01  10:00            15,234 custom_remote_desktop.py
2024/01/01  10:00               567 start_custom_remote_client.bat
               2 File(s)         15,801 bytes
```

**`custom_remote_desktop.py` が表示されればOKです。**

### ファイルが表示されない場合

ファイルが存在しない場合は、以下を確認してください:

1. **エクスプローラーでフォルダを確認**
   - `C:\Users\kouse\OneDrive\ドキュメント\デスクトップ\Remote Desktop` フォルダを開く
   - `custom_remote_desktop.py` が存在するか確認

2. **ファイルが存在しない場合**
   - デスクトップ側からファイルをコピーしてください

---

## 🚀 ステップ4: 正しいコマンドを実行

### コマンドの入力方法

1. **コマンドプロンプトに以下を入力**
   ```bash
   python custom_remote_desktop.py client 192.168.1.100
   ```
   （`192.168.1.100` を実際のIPアドレスに変更）

2. **Enterキーを押す**

3. **結果を確認**
   - GUIウィンドウが開くはずです
   - エラーメッセージが表示される場合は、内容を確認してください

---

## 🔍 ステップ5: エラーメッセージを確認

### エラーが表示された場合

エラーメッセージの内容を確認してください:

#### エラー1: 「can't open file」

**原因**: ファイルが見つからない

**対処法**:
- ファイルが正しいフォルダにあるか確認
- ファイル名が正しいか確認（`custom_remote_desktop.py`）

#### エラー2: 「ModuleNotFoundError」

**原因**: ライブラリがインストールされていない

**対処法**:
```bash
python -m pip install mss pillow pyautogui
```

#### エラー3: 「サーバーに接続できませんでした」

**原因**: デスクトップ側でサーバーが起動していない

**対処法**:
- デスクトップ側でサーバーが起動しているか確認
- IPアドレスが正しいか確認

---

## 💡 簡単な方法: バッチファイルを使用

### バッチファイルから実行

1. **エクスプローラーでフォルダを開く**
   - `C:\Users\kouse\OneDrive\ドキュメント\デスクトップ\Remote Desktop` フォルダを開く

2. **バッチファイルを実行**
   - `start_custom_remote_client.bat` をダブルクリック

3. **IPアドレスを入力**
   - プロンプトが表示されたら、IPアドレスを入力
   - Enterキーを押す

---

## 🔄 完全にリセットする方法

### 手順1: すべてのコマンドプロンプトを閉じる

1. 開いているすべてのコマンドプロンプトウィンドウを閉じる

### 手順2: 新しいコマンドプロンプトを開く

1. **Windowsキー + R** を押す
2. 「**cmd**」と入力してEnterキーを押す
3. 新しいコマンドプロンプトが開きます

### 手順3: フォルダに移動

コマンドプロンプトに以下を入力してEnterキーを押す:

```bash
cd "C:\Users\kouse\OneDrive\ドキュメント\デスクトップ\Remote Desktop"
```

**注意**: パスに日本語やスペースが含まれている場合は、ダブルクォート（`"`）で囲む必要があります。

### 手順4: ファイルを確認

```bash
dir custom_remote_desktop.py
```

### 手順5: クライアントを起動

```bash
python custom_remote_desktop.py client 192.168.1.100
```

---

## 📝 チェックリスト

- [ ] コマンドプロンプトを再起動した
- [ ] 正しいフォルダに移動した
- [ ] ファイルが存在するか確認した（`dir custom_remote_desktop.py`）
- [ ] 正しいコマンドを実行した（`python custom_remote_desktop.py client [IPアドレス]`）
- [ ] エラーメッセージを確認した

---

## 🆘 それでも解決しない場合

### デバッグ情報を収集

以下の情報をメモしてください:

1. **コマンドプロンプトに表示されている内容**
   - スクリーンショットを撮るか、内容をコピー

2. **実行したコマンド**
   - 例: `python custom_remote_desktop.py client 192.168.1.100`

3. **エラーメッセージ（あれば）**
   - エラーメッセージの全文

4. **ファイルの存在確認**
   - `dir custom_remote_desktop.py` の結果

5. **Pythonのバージョン**
   - `python --version` の結果

これらの情報があれば、さらに詳しくサポートできます。

---

## 📝 まとめ

1. ✅ コマンドプロンプトを再起動
2. ✅ 正しいフォルダに移動（`cd "C:\Users\kouse\OneDrive\ドキュメント\デスクトップ\Remote Desktop"`）
3. ✅ ファイルが存在するか確認（`dir custom_remote_desktop.py`）
4. ✅ 正しいコマンドを実行（`python custom_remote_desktop.py client 192.168.1.100`）

これで、問題が解決するはずです！





