# Chrome風リモートデスクトップ - 完全セットアップガイド

このガイドでは、Chrome Remote Desktop風のリモートデスクトップアプリを**ゼロから**使えるようになるまでの手順を詳しく説明します。

---

## 📋 目次

1. [準備（両方のPCで必要）](#準備両方のpcで必要)
2. [デスクトップ側（サーバー）の設定](#デスクトップ側サーバーの設定)
3. [ノートパソコン側（クライアント）の設定](#ノートパソコン側クライアントの設定)
4. [接続と使い方](#接続と使い方)
5. [トラブルシューティング](#トラブルシューティング)

---

## 🔧 準備（両方のPCで必要）

### ステップ1: プロジェクトフォルダを開く

1. **エクスプローラーを開く**
   - Windowsキー + E を押す

2. **プロジェクトフォルダに移動**
   - アドレスバーに以下を入力してEnter:
   ```
   C:\Users\西村康成\WasherInspection
   ```

3. **ファイルを確認**
   - `chrome_like_remote_desktop.py` があることを確認

---

### ステップ2: コマンドプロンプトを開く

#### 方法A: エクスプローラーから開く（最も簡単・推奨）

1. **エクスプローラーでプロジェクトフォルダを開いた状態で**
2. **アドレスバーをクリック**（上部のパスが表示されている場所）
3. **「cmd」と入力してEnterキーを押す**
4. **コマンドプロンプトがそのフォルダで開きます**

#### 方法B: Windowsキー + Rから開く

1. **Windowsキー + R** を同時に押す
2. **「cmd」と入力してEnterキーを押す**
3. **フォルダに移動**:
   ```bash
   cd "C:\Users\西村康成\WasherInspection"
   ```
4. **Enterキーを押す**

---

### ステップ3: Pythonがインストールされているか確認

1. **コマンドプロンプトに以下を入力**:
   ```bash
   python --version
   ```
   
2. **Enterキーを押す**

3. **結果を確認**:
   - ✅ **Pythonのバージョンが表示される場合**（例: `Python 3.11.5`）
     → 次のステップに進んでください
   - ❌ **「'python' は、内部コマンドまたは外部コマンド...」と表示される場合**
     → Pythonをインストールする必要があります

#### Pythonがインストールされていない場合

1. **Pythonをダウンロード**
   - https://www.python.org/downloads/ にアクセス
   - 「Download Python」ボタンをクリック

2. **インストール**
   - ダウンロードしたファイルを実行
   - ⚠️ **重要**: 「**Add Python to PATH**」に**必ずチェックを入れる**
   - 「Install Now」をクリック

3. **コマンドプロンプトを再起動**
   - コマンドプロンプトを閉じて、再度開く
   - `python --version` で確認

---

### ステップ4: 必要なライブラリをインストール

1. **コマンドプロンプトに以下を入力**:
   ```bash
   python -m pip install mss pillow pyautogui pyperclip cryptography
   ```

2. **Enterキーを押す**

3. **インストールが完了するまで待つ**
   - 「Successfully installed...」と表示されれば完了
   - エラーが出た場合は、次のコマンドを試してください:
     ```bash
     py -m pip install mss pillow pyautogui pyperclip cryptography
     ```

4. **インストール確認**（オプション）:
   ```bash
   python -m pip list | findstr mss
   python -m pip list | findstr pillow
   python -m pip list | findstr pyautogui
   ```
   - それぞれのライブラリ名が表示されればOK

---

## 🖥️ デスクトップ側（サーバー）の設定

### ステップ1: IPアドレスを確認

1. **コマンドプロンプトを開く**（既に開いている場合はそのまま）

2. **以下を入力**:
   ```bash
   ipconfig
   ```

3. **Enterキーを押す**

4. **IPv4アドレスをメモ**
   - 「IPv4 アドレス」の横に表示される数字（例: `192.168.1.100`）
   - このIPアドレスを**ノートパソコン側で使用します**

---

### ステップ2: サーバーを起動

#### 方法A: バッチファイルから起動（簡単）

1. **エクスプローラーでプロジェクトフォルダを開く**
2. **`start_chrome_like_server.bat` をダブルクリック**

#### 方法B: コマンドプロンプトから起動

1. **コマンドプロンプトを開く**（プロジェクトフォルダで）

2. **以下を入力**:
   ```bash
   python chrome_like_remote_desktop.py server
   ```

3. **Enterキーを押す**

---

### ステップ3: パスワードを確認

サーバーが起動すると、以下のようなメッセージが表示されます:

```
=== Chrome風リモートデスクトップサーバー ===
[サーバー] パスワード: xYz123AbC456DeF
[サーバー] パスワードハッシュ: a1b2c3d4e5f6...
[サーバー] 待機中: 0.0.0.0:8888
```

⚠️ **重要**: 
- **パスワードをメモしてください**（例: `xYz123AbC456DeF`）
- このパスワードは**ノートパソコン側で接続するときに必要**です
- パスワードは**毎回異なります**（自動生成されるため）

---

### ステップ4: 接続を待つ

- **「[サーバー] 待機中: 0.0.0.0:8888」** と表示されていればOK
- **このウィンドウは閉じないでください**
- ノートパソコン側から接続されるまで待機します

---

## 💻 ノートパソコン側（クライアント）の設定

### ステップ1: デスクトップ側のIPアドレスを確認

- デスクトップ側で確認したIPアドレスをメモ（例: `192.168.1.100`）

---

### ステップ2: クライアントを起動

#### 方法A: バッチファイルから起動（簡単）

1. **エクスプローラーでプロジェクトフォルダを開く**
2. **`start_chrome_like_client.bat` をダブルクリック**
3. **GUIが開きます**

#### 方法B: コマンドプロンプトから起動

1. **コマンドプロンプトを開く**（プロジェクトフォルダで）

2. **以下を入力**（IPアドレスを変更してください）:
   ```bash
   python chrome_like_remote_desktop.py client 192.168.1.100
   ```
   - `192.168.1.100` の部分を、デスクトップ側のIPアドレスに変更

3. **Enterキーを押す**

4. **GUIが開きます**

#### 方法C: GUIモードから起動

1. **コマンドプロンプトを開く**

2. **以下を入力**:
   ```bash
   python chrome_like_remote_desktop.py
   ```

3. **Enterキーを押す**

4. **「クライアント（ノートパソコン側）」ボタンをクリック**

---

### ステップ3: 接続設定

1. **GUIが開いたら、ツールバーの「サーバー」欄にIPアドレスを入力**
   - 例: `192.168.1.100`

2. **「ポート」欄を確認**（通常は `8888` のまま）

3. **「接続」ボタンをクリック**

---

### ステップ4: パスワード認証

1. **パスワード入力ダイアログが表示されます**

2. **デスクトップ側で表示されたパスワードを入力**
   - 例: `xYz123AbC456DeF`
   - ⚠️ **大文字小文字を区別します**

3. **「OK」ボタンをクリック**

4. **認証が成功すると、リモート画面が表示されます**

---

## 🎮 接続と使い方

### 基本的な操作

- **マウス操作**: リモート画面上でマウスを動かすと、デスクトップ側のマウスが動きます
- **クリック**: 左クリック、右クリック、ダブルクリックが可能
- **キーボード入力**: リモート画面にフォーカスを当てて、キーボードで入力できます
- **スクロール**: マウスホイールでスクロールできます

---

### ファイル転送

#### クライアント側からサーバー側へファイルを送信

1. **ツールバーの「送信」ボタンをクリック**
2. **送信したいファイルを選択**
3. **ファイルがサーバー側の `Downloads` フォルダに保存されます**

#### サーバー側からクライアント側へファイルを受信

1. **ツールバーの「受信」ボタンをクリック**
2. **サーバー側のファイルパスを入力**（例: `C:\Users\西村康成\Documents\file.txt`）
3. **ファイルがダウンロードされます**

---

### クリップボード共有

- **自動的に同期されます**
- クライアント側でコピーしたテキストが、サーバー側のクリップボードに自動的にコピーされます
- サーバー側でコピーしたテキストが、クライアント側のクリップボードに自動的にコピーされます

---

### 画面品質の調整

1. **ツールバーの「品質」スライダーを動かす**
   - 左（30%）: 低品質・高速
   - 右（100%）: 高品質・低速
   - 推奨: 75%（バランス型）

---

### 切断

- **「切断」ボタンをクリック**すると、接続が切れます

---

## 🔧 トラブルシューティング

### 問題1: 「サーバーに接続できませんでした」と表示される

#### 確認事項

1. **デスクトップ側のサーバーが起動しているか**
   - デスクトップ側のコマンドプロンプトで「[サーバー] 待機中...」と表示されているか確認

2. **IPアドレスが正しいか**
   - デスクトップ側で `ipconfig` を実行して、IPv4アドレスを再確認
   - クライアント側で入力したIPアドレスと一致しているか確認

3. **同じネットワークに接続されているか**
   - 両方のPCが同じWiFiまたは有線LANに接続されているか確認

4. **ファイアウォールがブロックしていないか**
   - Windowsファイアウォールでポート8888がブロックされていないか確認
   - 一時的にファイアウォールを無効化して試す

#### 解決方法

1. **デスクトップ側のファイアウォール設定を変更**:
   - Windowsセキュリティ → ファイアウォールとネットワーク保護
   - 「詳細設定」をクリック
   - 「受信の規則」→「新しい規則」→「ポート」→「TCP」→「特定のローカルポート: 8888」→「接続を許可する」

2. **サーバーを再起動**:
   - デスクトップ側でサーバーを停止（Ctrl+C）して、再度起動

3. **クライアントを再起動**:
   - クライアント側で接続を試みる

---

### 問題2: 「認証に失敗しました」と表示される

#### 確認事項

1. **パスワードが正しいか**
   - デスクトップ側で表示されたパスワードを**正確に**入力しているか確認
   - 大文字小文字を区別します
   - スペースや改行が入っていないか確認

2. **パスワードが古くないか**
   - サーバーを再起動すると、パスワードが変わります
   - 最新のパスワードを使用しているか確認

#### 解決方法

1. **デスクトップ側でパスワードを再確認**
   - サーバー起動時に表示されたパスワードを確認

2. **クライアント側で再度接続**
   - 正しいパスワードを入力

---

### 問題3: 画面が表示されない

#### 確認事項

1. **mssライブラリがインストールされているか**
   ```bash
   python -m pip list | findstr mss
   ```
   - 表示されない場合は、インストール:
     ```bash
     python -m pip install mss
     ```

2. **画面キャプチャの権限があるか**
   - Windows: 画面キャプチャの権限が必要な場合があります

#### 解決方法

1. **ライブラリを再インストール**:
   ```bash
   python -m pip install --upgrade mss pillow
   ```

2. **サーバーを再起動**

---

### 問題4: リモート制御が動作しない

#### 確認事項

1. **pyautoguiライブラリがインストールされているか**
   ```bash
   python -m pip list | findstr pyautogui
   ```

2. **マウス/キーボード制御の権限があるか**

#### 解決方法

1. **ライブラリを再インストール**:
   ```bash
   python -m pip install --upgrade pyautogui
   ```

2. **サーバーを再起動**

---

### 問題5: ファイル転送ができない

#### 確認事項

1. **ファイルパスが正しいか**
   - サーバー側のファイルパスが存在するか確認

2. **権限があるか**
   - ファイルにアクセスする権限があるか確認

#### 解決方法

1. **ファイルパスを再確認**
   - 絶対パスを使用（例: `C:\Users\西村康成\Documents\file.txt`）

2. **ファイル名に日本語が含まれている場合**
   - 英数字のファイル名に変更して試す

---

### 問題6: Pythonがインストールされていない

#### 解決方法

1. **Pythonをダウンロード**
   - https://www.python.org/downloads/

2. **インストール**
   - 「Add Python to PATH」にチェックを入れる

3. **コマンドプロンプトを再起動**

4. **確認**:
   ```bash
   python --version
   ```

---

## 📝 チェックリスト

### デスクトップ側

- [ ] Pythonがインストールされている
- [ ] 必要なライブラリがインストールされている
- [ ] IPアドレスを確認した
- [ ] サーバーを起動した
- [ ] パスワードをメモした
- [ ] 「待機中...」と表示されている

### ノートパソコン側

- [ ] Pythonがインストールされている
- [ ] 必要なライブラリがインストールされている
- [ ] デスクトップ側のIPアドレスを確認した
- [ ] クライアントを起動した
- [ ] 接続ボタンをクリックした
- [ ] パスワードを入力した
- [ ] リモート画面が表示された

---

## 💡 よくある質問

### Q: パスワードを忘れた場合は？

A: サーバーを再起動すると、新しいパスワードが生成されます。デスクトップ側でサーバーを停止（Ctrl+C）して、再度起動してください。

### Q: ポート番号を変更できますか？

A: はい、可能です。サーバー側とクライアント側で同じポート番号を指定してください。コード内の `port=8888` の部分を変更するか、コマンドライン引数で指定できます。

### Q: インターネット経由で接続できますか？

A: 現在の実装では、同じネットワーク内での接続のみを想定しています。インターネット経由で接続する場合は、VPNを使用するか、ポートフォワーディングを設定する必要があります。

### Q: 複数のクライアントから接続できますか？

A: 現在の実装では、1つのクライアントのみ接続可能です。複数クライアント対応は今後の改善予定です。

---

## 🎯 まとめ

1. **準備**: Pythonとライブラリをインストール
2. **デスクトップ側**: サーバーを起動してパスワードを確認
3. **ノートパソコン側**: クライアントを起動して接続
4. **認証**: パスワードを入力
5. **使用**: リモートデスクトップを操作

---

**問題が解決しない場合は、エラーメッセージを確認して、上記のトラブルシューティングを参照してください。**




