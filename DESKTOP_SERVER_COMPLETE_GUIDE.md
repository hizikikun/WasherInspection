# デスクトップ側（サーバー）完全セットアップガイド

デスクトップ側で最初から必要な操作を順番に説明します。

---

## 📋 目次

1. [Pythonのインストール確認](#pythonのインストール確認)
2. [プロジェクトファイルの確認](#プロジェクトファイルの確認)
3. [必要なライブラリのインストール](#必要なライブラリのインストール)
4. [IPアドレスの確認](#ipアドレスの確認)
5. [サーバーの起動](#サーバーの起動)
6. [パスワードの確認](#パスワードの確認)
7. [接続を待つ](#接続を待つ)

---

## ✅ ステップ1: Pythonのインストール確認

### 1-1. Pythonがインストールされているか確認

1. **コマンドプロンプトを開く**
   - Windowsキー + R → 「cmd」と入力 → Enter

2. **以下を実行**:
   ```bash
   python --version
   ```

3. **結果を確認**:
   - ✅ **Pythonのバージョンが表示される場合**（例: `Python 3.13.9`）
     → 次のステップに進んでください
   - ❌ **「'python' は、内部コマンドまたは外部コマンド...」と表示される場合**
     → Pythonをインストールする必要があります

### 1-2. Pythonがインストールされていない場合

1. **Pythonをダウンロード**
   - https://www.python.org/downloads/ にアクセス
   - 「Download Python」ボタンをクリック

2. **インストール**
   - ダウンロードしたファイルを実行
   - ⚠️ **重要**: 「**Add Python to PATH**」に**必ずチェックを入れる**
   - 「Install Now」をクリック

3. **コマンドプロンプトを再起動**
   - 既に開いているコマンドプロンプトを閉じて、再度開く

4. **確認**:
   ```bash
   python --version
   ```

---

## 📁 ステップ2: プロジェクトファイルの確認

### 2-1. プロジェクトフォルダを開く

1. **エクスプローラーを開く**
   - Windowsキー + E

2. **プロジェクトフォルダに移動**
   - アドレスバーに以下を入力:
   ```
   C:\Users\西村康成\WasherInspection
   ```

### 2-2. 必要なファイルを確認

以下のファイルが存在することを確認:
- ✅ `chrome_like_remote_desktop.py`（必須）
- ✅ `start_chrome_like_server.bat`（オプション、推奨）

---

## 📦 ステップ3: 必要なライブラリのインストール

### 3-1. プロジェクトフォルダでコマンドプロンプトを開く

1. **エクスプローラーでプロジェクトフォルダを開いた状態で**
2. **アドレスバーに「cmd」と入力してEnter**
   - そのフォルダでコマンドプロンプトが開きます

または、コマンドプロンプトで:

```bash
cd "C:\Users\西村康成\WasherInspection"
```

### 3-2. ライブラリをインストール

1. **pipをアップグレード**（推奨）:
   ```bash
   python -m pip install --upgrade pip
   ```

2. **必要なライブラリをインストール**:
   ```bash
   python -m pip install mss pillow pyautogui pyperclip cryptography
   ```

3. **インストールが完了するまで待つ**
   - 「Successfully installed...」と表示されれば完了

### 3-3. インストール確認（オプション）

```bash
python -m pip list | findstr "mss pillow pyautogui pyperclip cryptography"
```

すべてのライブラリが表示されればOKです。

---

## 🔍 ステップ4: IPアドレスの確認

### 4-1. IPアドレスを確認

1. **コマンドプロンプトを開く**

2. **以下を実行**:
   ```bash
   ipconfig
   ```

3. **IPv4アドレスをメモ**
   - 「IPv4 アドレス」の横に表示される数字
   - 例: `192.168.1.100`
   - ⚠️ **このIPアドレスをノートパソコン側で使用します**

### 4-2. IPアドレスが変わらないようにする（オプション）

IPアドレスが頻繁に変わる場合は、固定IPアドレスに設定することを推奨します。

---

## 🚀 ステップ5: サーバーの起動

### 5-1. サーバーを起動

#### 方法A: バッチファイルから起動（簡単）

1. **エクスプローラーでプロジェクトフォルダを開く**

2. **`start_chrome_like_server.bat` をダブルクリック**
   - コマンドプロンプトが開きます

#### 方法B: コマンドプロンプトから起動

1. **プロジェクトフォルダでコマンドプロンプトを開く**

2. **以下を実行**:
   ```bash
   python chrome_like_remote_desktop.py server
   ```

3. **コマンドプロンプトにメッセージが表示されます**

---

## 🔐 ステップ6: パスワードの確認

### 6-1. パスワードを確認

サーバーが起動すると、以下のようなメッセージが表示されます:

```
=== Chrome風リモートデスクトップサーバー ===
[サーバー] パスワード: xYz123AbC456DeF
[サーバー] パスワードハッシュ: a1b2c3d4e5f6...
[サーバー] 待機中: 0.0.0.0:8888
```

⚠️ **重要**: 
- **パスワードをメモしてください**（例: `xYz123AbC456DeF`）
- このパスワードは**ノートパソコン側で接続するときに必要**です
- パスワードは**毎回異なります**（自動生成されるため）

### 6-2. パスワードを表示する方法

パスワードが画面から消えてしまった場合:

1. **サーバーを停止**（Ctrl+C）
2. **サーバーを再起動**
3. **新しいパスワードが表示されます**

---

## ⏳ ステップ7: 接続を待つ

### 7-1. サーバーの状態を確認

サーバーが正常に起動している場合、以下のメッセージが表示されます:

```
[サーバー] 待機中: 0.0.0.0:8888
```

この状態で、ノートパソコン側からの接続を待っています。

### 7-2. サーバーを起動したままにする

⚠️ **重要**: 
- **このコマンドプロンプトのウィンドウは閉じないでください**
- サーバーを停止するには、Ctrl+C を押します
- ウィンドウを閉じると、サーバーが停止します

### 7-3. 接続が来たとき

ノートパソコン側から接続が来ると、以下のようなメッセージが表示されます:

```
[サーバー] クライアント接続: ('192.168.1.8', 54321)
[サーバー] SSL接続確立
[サーバー] 認証成功
```

---

## 📝 チェックリスト

### セットアップ

- [ ] Pythonがインストールされている（`python --version` で確認）
- [ ] プロジェクトファイル（`chrome_like_remote_desktop.py`）が存在する
- [ ] 必要なライブラリをインストールした
- [ ] IPアドレスを確認した

### サーバー起動

- [ ] サーバーを起動した
- [ ] パスワードをメモした
- [ ] 「待機中...」と表示されている
- [ ] コマンドプロンプトのウィンドウを閉じていない

---

## 🔧 トラブルシューティング

### 問題1: サーバーが起動しない

#### 確認事項

1. **Pythonが正しくインストールされているか**
   ```bash
   python --version
   ```

2. **必要なライブラリがインストールされているか**
   ```bash
   python -m pip list | findstr "mss pillow pyautogui pyperclip cryptography"
   ```

3. **ファイルが存在するか**
   ```bash
   dir chrome_like_remote_desktop.py
   ```

#### 解決方法

1. **ライブラリを再インストール**:
   ```bash
   python -m pip install mss pillow pyautogui pyperclip cryptography
   ```

2. **サーバーを再起動**

---

### 問題2: ポートが既に使用されている

#### エラーメッセージ

```
[Errno 10048] 通常、各ソケット アドレス (プロトコル/ネットワーク アドレス/ポート) は一度だけ使用できます。
```

#### 解決方法

1. **他のプログラムがポート8888を使用していないか確認**

2. **ポートを変更する**（コードを編集）:
   ```python
   server = SecureRemoteDesktopServer(host='0.0.0.0', port=8889)  # 8888から8889に変更
   ```

3. **サーバーを再起動**

---

### 問題3: ファイアウォールがブロックしている

#### 解決方法

1. **Windowsセキュリティを開く**
   - Windowsセキュリティ → ファイアウォールとネットワーク保護

2. **「詳細設定」をクリック**

3. **「受信の規則」→「新しい規則」をクリック**

4. **「ポート」を選択 →「次へ」**

5. **「TCP」を選択 →「特定のローカルポート: 8888」→「次へ」**

6. **「接続を許可する」を選択 →「次へ」**

7. **すべてのプロファイルにチェック →「次へ」**

8. **名前を入力（例: 「リモートデスクトップサーバー」）→「完了」**

---

## 🎯 まとめ

デスクトップ側で必要な操作:

1. **Pythonのインストール確認**: `python --version`
2. **プロジェクトファイルの確認**: `chrome_like_remote_desktop.py` が存在するか
3. **ライブラリのインストール**: `python -m pip install mss pillow pyautogui pyperclip cryptography`
4. **IPアドレスの確認**: `ipconfig` → IPv4アドレスをメモ
5. **サーバーの起動**: `python chrome_like_remote_desktop.py server`
6. **パスワードの確認**: 表示されたパスワードをメモ
7. **接続を待つ**: 「待機中...」と表示されていることを確認

---

**準備ができたら、サーバーを起動してノートパソコン側からの接続を待ちましょう！**



