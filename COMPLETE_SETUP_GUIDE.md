# 完全セットアップガイド（最初から最後まで）

## 📋 全体の流れ

```
[デスクトップ側]                    [ノートパソコン側]
     ↓                                      ↓
1. フォルダ作成                   1. フォルダ作成
     ↓                                      ↓
2. ファイル配置                   2. ファイル配置
     ↓                                      ↓
3. ライブラリインストール         3. ライブラリインストール
     ↓                                      ↓
4. サーバー起動                   4. IPアドレス確認
     ↓                                      ↓
5. 接続待機 ←─────────────────── 5. クライアント起動
     ↓                                      ↓
6. 接続成功！                     6. 接続成功！
```

---

## 🖥️ デスクトップ側（自宅のPC）の手順

### ステップ1: フォルダを作成

1. **エクスプローラーを開く**
   - Windowsキー + E を押す
   - または、タスクバーのフォルダアイコンをクリック

2. **デスクトップに移動**
   - 左側のサイドバーで「デスクトップ」をクリック

3. **新しいフォルダを作成**
   - デスクトップの空白部分を右クリック
   - 「新規作成」→「フォルダ」を選択
   - フォルダ名を入力（例: `RemoteDesktop`）
   - Enterキーを押す

### ステップ2: 必要なファイルをコピー

1. **WasherInspectionフォルダを開く**
   - エクスプローラーで `C:\Users\西村康成\WasherInspection` を開く

2. **ファイルをコピー**
   - `custom_remote_desktop.py` を右クリック → 「コピー」
   - `start_custom_remote_server.bat` を右クリック → 「コピー」

3. **作成したフォルダに貼り付け**
   - デスクトップの `RemoteDesktop` フォルダを開く
   - 右クリック → 「貼り付け」

### ステップ3: コマンドプロンプトを開く

1. **エクスプローラーでフォルダを開く**
   - デスクトップの `RemoteDesktop` フォルダを開く

2. **アドレスバーに「cmd」と入力**
   - フォルダを開いた状態で、上部のアドレスバーをクリック
   - 「**cmd**」と入力してEnterキーを押す

3. **コマンドプロンプトが開く**
   - そのフォルダでコマンドプロンプトが開きます
   - パスが `C:\Users\西村康成\Desktop\RemoteDesktop>` のように表示されます

### ステップ4: ライブラリをインストール

コマンドプロンプトに以下を入力してEnterキーを押す:

```bash
python -m pip install mss pillow pyautogui
```

**実行結果の例:**
```
Collecting mss
  Downloading mss-9.0.1-py3-none-any.whl
...
Successfully installed mss-9.0.1 pillow-10.1.0 pyautogui-0.9.54
```

### ステップ5: IPアドレスを確認

コマンドプロンプトに以下を入力してEnterキーを押す:

```bash
ipconfig
```

**表示される内容の例:**
```
Windows IP構成

イーサネット アダプター イーサネット:

   IPv4 アドレス . . . . . . . . . . . : 192.168.1.100
```

**「IPv4 アドレス」の右側の数字をメモしてください。**
例: `192.168.1.100`

**このIPアドレスをノートパソコン側で使用します。**

### ステップ6: サーバーを起動

#### 方法A: バッチファイルから起動（簡単）

1. エクスプローラーで `RemoteDesktop` フォルダを開く
2. `start_custom_remote_server.bat` を**ダブルクリック**

#### 方法B: コマンドプロンプトから起動

コマンドプロンプトに以下を入力してEnterキーを押す:

```bash
python custom_remote_desktop.py server
```

**表示される内容:**
```
=== リモートデスクトップサーバー ===
デスクトップ側で実行してください
クライアントの接続を待機しています...
[サーバー] 待機中: 0.0.0.0:8888
```

**この状態で待機します。** ノートパソコン側からの接続を待っています。

**このコマンドプロンプトウィンドウは閉じないでください。**

---

## 💻 ノートパソコン側（大学のPC）の手順

### ステップ1: フォルダを作成

1. **エクスプローラーを開く**
   - Windowsキー + E を押す
   - または、タスクバーのフォルダアイコンをクリック

2. **デスクトップに移動**
   - 左側のサイドバーで「デスクトップ」をクリック

3. **新しいフォルダを作成**
   - デスクトップの空白部分を右クリック
   - 「新規作成」→「フォルダ」を選択
   - フォルダ名を入力（例: `RemoteDesktop`）
   - Enterキーを押す

### ステップ2: 必要なファイルをコピー

1. **デスクトップ側からファイルを取得**
   - USBメモリ、クラウドストレージ、メールなどでファイルを転送
   - または、デスクトップ側の `RemoteDesktop` フォルダから直接コピー

2. **ファイルをコピー**
   - `custom_remote_desktop.py` をコピー
   - `start_custom_remote_client.bat` をコピー

3. **作成したフォルダに貼り付け**
   - ノートパソコンのデスクトップの `RemoteDesktop` フォルダを開く
   - 右クリック → 「貼り付け」

### ステップ3: コマンドプロンプトを開く

1. **エクスプローラーでフォルダを開く**
   - デスクトップの `RemoteDesktop` フォルダを開く

2. **アドレスバーに「cmd」と入力**
   - フォルダを開いた状態で、上部のアドレスバーをクリック
   - 「**cmd**」と入力してEnterキーを押す

3. **コマンドプロンプトが開く**
   - そのフォルダでコマンドプロンプトが開きます

### ステップ4: Pythonがインストールされているか確認

コマンドプロンプトに以下を入力してEnterキーを押す:

```bash
python --version
```

**表示される内容の例:**
```
Python 3.11.5
```

**Pythonが表示されない場合:**
- Pythonをインストールする必要があります
- https://www.python.org/downloads/ からダウンロード
- インストール時に「**Add Python to PATH**」にチェックを入れる

### ステップ5: ライブラリをインストール

コマンドプロンプトに以下を入力してEnterキーを押す:

```bash
python -m pip install mss pillow pyautogui
```

**実行結果の例:**
```
Collecting mss
  Downloading mss-9.0.1-py3-none-any.whl
...
Successfully installed mss-9.0.1 pillow-10.1.0 pyautogui-0.9.54
```

### ステップ6: デスクトップ側のIPアドレスを確認

デスクトップ側で確認したIPアドレスをメモしてください。

例: `192.168.1.100`

### ステップ7: クライアントを起動

#### 方法A: バッチファイルから起動（簡単）

1. **エクスプローラーでフォルダを開く**
   - デスクトップの `RemoteDesktop` フォルダを開く

2. **バッチファイルを実行**
   - `start_custom_remote_client.bat` を**ダブルクリック**

3. **IPアドレスを入力**
   - コマンドプロンプトに以下のように表示されます:
     ```
     サーバーIPアドレスを入力してください (デフォルト: 192.168.1.100): 
     ```
   - デスクトップ側で確認したIPアドレスを入力（例: `192.168.1.100`）
   - Enterキーを押す

4. **「Press any key to continue...」と表示されたら**
   - 何かキーを押す（Enterキーなど）
   - コマンドプロンプトウィンドウが閉じます

5. **GUIウィンドウが開く**
   - リモートデスクトップのGUIウィンドウが開きます
   - デスクトップ側の画面が表示されます

#### 方法B: コマンドプロンプトから起動

1. **コマンドプロンプトを開く**
   - エクスプローラーで `RemoteDesktop` フォルダを開く
   - アドレスバーに「cmd」と入力してEnter

2. **クライアントを起動**
   - コマンドプロンプトに以下を入力してEnterキーを押す:
     ```bash
     python custom_remote_desktop.py client 192.168.1.100
     ```
     （`192.168.1.100` の部分を、デスクトップ側で確認したIPアドレスに変更）

3. **GUIウィンドウが開く**
   - リモートデスクトップのGUIウィンドウが開きます
   - デスクトップ側の画面が表示されます

### ステップ8: 接続成功の確認

#### 正常に接続できた場合

1. **GUIウィンドウが開く**
   - タイトル: 「リモートデスクトップ - クライアント」
   - サイズ: 1024x768（デフォルト）

2. **接続状態の表示**
   - 上部に「接続中: 192.168.1.100:8888」と表示
   - その後、「接続成功: 192.168.1.100」と表示

3. **デスクトップの画面が表示される**
   - ウィンドウ内に、デスクトップ側の画面が表示されます
   - リアルタイムで更新されます

#### 操作方法

- **マウス操作**: ウィンドウ内でマウスを動かすと、デスクトップ側のマウスが動きます
- **クリック**: 画面をクリックすると、デスクトップ側でクリックされます
- **キーボード**: キーを押すと、デスクトップ側で入力されます
- **右クリック**: 画面を右クリックすると、デスクトップ側で右クリックされます

---

## 🔴 エラーが発生した場合

### エラー1: 「'python' は、内部コマンドまたは外部コマンド...」

#### 原因
Pythonがインストールされていない、またはPATHに追加されていない

#### 対処法
1. Pythonをインストール: https://www.python.org/downloads/
2. インストール時に「**Add Python to PATH**」にチェックを入れる
3. インストール後、コマンドプロンプトを再起動

### エラー2: 「'pip' は、内部コマンドまたは外部コマンド...」

#### 原因
pipが使えない

#### 対処法
`python -m pip` を使用:
```bash
python -m pip install mss pillow pyautogui
```

### エラー3: 「サーバーに接続できませんでした」

#### 原因
- デスクトップ側でサーバーが起動していない
- IPアドレスが間違っている
- ファイアウォールがブロックしている

#### 対処法
1. デスクトップ側でサーバーが起動しているか確認
2. IPアドレスが正しいか確認（`ipconfig` で再確認）
3. ファイアウォールの設定を確認

---

## 📋 チェックリスト

### デスクトップ側
- [ ] フォルダを作成した
- [ ] `custom_remote_desktop.py` をコピーした
- [ ] `start_custom_remote_server.bat` をコピーした
- [ ] ライブラリをインストールした（`python -m pip install mss pillow pyautogui`）
- [ ] IPアドレスを確認した（`ipconfig`）
- [ ] サーバーを起動した（`python custom_remote_desktop.py server`）

### ノートパソコン側
- [ ] フォルダを作成した
- [ ] `custom_remote_desktop.py` をコピーした
- [ ] `start_custom_remote_client.bat` をコピーした
- [ ] Pythonがインストールされている（`python --version`）
- [ ] ライブラリをインストールした（`python -m pip install mss pillow pyautogui`）
- [ ] デスクトップ側のIPアドレスを確認した
- [ ] クライアントを起動した（`python custom_remote_desktop.py client [IPアドレス]`）
- [ ] GUIウィンドウが開いた
- [ ] デスクトップの画面が表示された

---

## 📝 まとめ

### デスクトップ側
1. フォルダ作成 → ファイル配置 → ライブラリインストール → IPアドレス確認 → サーバー起動

### ノートパソコン側
1. フォルダ作成 → ファイル配置 → ライブラリインストール → クライアント起動 → 接続成功

これで、リモートデスクトップ接続が完了です！





