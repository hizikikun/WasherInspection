# Chrome Remote Desktop風リモートデスクトップシステム

Google Chrome Remote Desktopと同等の機能を持つリモートデスクトップアプリケーションです。

## 🎯 主な機能

### ✅ 実装済み機能

- **🔒 暗号化通信（TLS/SSL）**: 通信を暗号化してセキュアに接続
- **🔐 パスワード認証**: 接続時にパスワード認証を要求
- **📺 リアルタイム画面共有**: 高品質な画面共有（15 FPS、品質調整可能）
- **🖱️ リモート制御**: マウスとキーボードの完全なリモート制御
  - マウス移動、クリック、ドラッグ、スクロール
  - キーボード入力、キーコンビネーション
- **📁 ファイル転送**: ファイルの送受信機能
- **📋 クリップボード共有**: クリップボードの自動同期
- **⚡ パフォーマンス最適化**:
  - 差分更新（変更部分のみ送信）
  - 画像圧縮（JPEG + zlib）
  - フレームレート調整
- **🔄 再接続機能**: 接続が切れた場合の自動再接続（準備中）

## 📋 必要なもの

### インストールが必要なライブラリ

```bash
pip install mss pillow pyautogui pyperclip cryptography
```

または、requirements.txtからインストール：

```bash
pip install -r requirements.txt
```

### 必要なライブラリの説明

- **mss**: 画面キャプチャ（Windows、macOS、Linux対応）
- **pillow**: 画像処理
- **pyautogui**: マウス/キーボード制御
- **pyperclip**: クリップボード操作
- **cryptography**: 暗号化通信（TLS/SSL）

## 🚀 使い方

### デスクトップ側（サーバー）

1. **サーバーを起動**
   ```bash
   python chrome_like_remote_desktop.py server
   ```

2. **パスワードを確認**
   - 起動時にランダムなパスワードが生成されます
   - このパスワードをクライアント側で入力します
   - 例: `[サーバー] パスワード: xYz123AbC456DeF`

3. **クライアントの接続を待機**
   - 画面に「待機中: 0.0.0.0:8888」と表示されます

### ノートパソコン側（クライアント）

1. **クライアントを起動**
   ```bash
   python chrome_like_remote_desktop.py client [デスクトップのIPアドレス]
   ```
   または
   ```bash
   python chrome_like_remote_desktop.py
   ```
   を実行してGUIから接続

2. **接続**
   - サーバーアドレスを入力（例: `192.168.1.100`）
   - 「接続」ボタンをクリック

3. **認証**
   - パスワード入力ダイアログが表示されます
   - サーバー側で表示されたパスワードを入力

4. **リモート制御**
   - 画面が表示されたら、マウスとキーボードで操作できます

## 🎨 GUI機能

### ツールバー

- **接続設定**: サーバーアドレス、ポート、接続/切断ボタン
- **ファイル転送**: ファイルの送信/受信ボタン
- **品質設定**: 画面品質のスライダー（30-100%）

### 画面表示

- **リモート画面**: リモートデスクトップの画面を表示
- **マウス操作**: クリック、ドラッグ、スクロールに対応
- **キーボード操作**: すべてのキー入力に対応

## ⚙️ 設定

### サーバー側の設定

```python
server = SecureRemoteDesktopServer(
    host='0.0.0.0',      # 接続を待つアドレス
    port=8888,          # ポート番号
    password=None       # パスワード（Noneの場合は自動生成）
)
```

### パフォーマンス設定

```python
# 画面品質（1-100）
server.quality = 75

# フレームレート（FPS）
server.fps = 15

# 差分更新を使用するか
server.use_diff = True
```

### ポート番号の変更

デフォルトポートは `8888` です。変更する場合は、サーバーとクライアントの両方で同じポートを指定してください。

## 🔒 セキュリティ

### 現在の実装

- **暗号化通信**: TLS/SSLを使用（簡易版 - 自己署名証明書）
- **パスワード認証**: SHA-256ハッシュを使用
- **接続制限**: 1つのクライアントのみ接続可能

### セキュリティの注意事項

⚠️ **本番環境での使用について**:
- 現在の実装は簡易版です
- 本番環境では適切なSSL証明書を使用してください
- 強力なパスワードを使用してください
- 信頼できるネットワークでのみ使用してください

### セキュリティを向上させるには

1. **適切なSSL証明書を使用**
   ```python
   # 本番環境では適切な証明書を設定
   context.load_cert_chain('server.crt', 'server.key')
   ```

2. **強力なパスワードを設定**
   ```python
   server = SecureRemoteDesktopServer(
       password='your-strong-password-here'
   )
   ```

3. **ファイアウォール設定**
   - 特定のIPアドレスのみ接続を許可
   - ポートを変更

4. **VPNを使用**
   - インターネット経由で接続する場合はVPNを使用

## 📊 パフォーマンス

### 推奨設定

- **画面品質**: 75%（バランス型）
- **フレームレート**: 15 FPS（標準）
- **ネットワーク**: 有線接続推奨（WiFiでも動作可能）

### 最適化のヒント

1. **画面品質を下げる**: ネットワークが遅い場合
   - 品質スライダーを30-50%に設定

2. **フレームレートを下げる**: CPU使用率が高い場合
   ```python
   server.fps = 10  # 10 FPSに変更
   ```

3. **差分更新を無効化**: 画面が頻繁に変化する場合
   ```python
   server.use_diff = False
   ```

## 🔄 Chrome Remote Desktopとの比較

| 機能 | このアプリ | Chrome Remote Desktop |
|------|-----------|----------------------|
| 画面共有 | ✅ | ✅ |
| リモート制御 | ✅ | ✅ |
| 暗号化通信 | ✅（簡易版） | ✅ |
| パスワード認証 | ✅ | ✅ |
| ファイル転送 | ✅ | ✅ |
| クリップボード共有 | ✅ | ✅ |
| 音声転送 | ❌ | ✅ |
| 複数モニター対応 | ⚠️ 準備中 | ✅ |
| NAT/ファイアウォール通過 | ⚠️ 要設定 | ✅ |
| 無料 | ✅ | ✅ |

## 💡 使用例

### 例1: 同じネットワーク内で接続

1. **デスクトップ側**
   ```bash
   python chrome_like_remote_desktop.py server
   ```
   - パスワードをメモ: `xYz123AbC456DeF`

2. **ノートパソコン側**
   ```bash
   python chrome_like_remote_desktop.py client 192.168.1.100
   ```
   - パスワードを入力: `xYz123AbC456DeF`

### 例2: ファイル転送

1. **クライアント側でファイルを送信**
   - ツールバーの「送信」ボタンをクリック
   - ファイルを選択
   - サーバー側の`Downloads`フォルダに保存されます

2. **サーバー側からファイルを受信**
   - クライアント側で「受信」ボタンをクリック
   - サーバー側のファイルパスを入力

### 例3: クリップボード共有

- クライアント側でコピーしたテキストが自動的にサーバー側に同期されます
- サーバー側でコピーしたテキストが自動的にクライアント側に同期されます

## 🔧 トラブルシューティング

### 接続できない場合

1. **ファイアウォールの確認**
   - ポート8888がブロックされていないか確認
   - Windowsファイアウォールでポートを開放

2. **IPアドレスの確認**
   - デスクトップ側のIPアドレスが正しいか確認
   - `ipconfig`（Windows）で確認

3. **ネットワーク接続の確認**
   - 両方のPCが同じネットワークに接続されているか確認

### 画面が表示されない場合

1. **mssライブラリの確認**
   ```bash
   pip install mss
   ```

2. **画面キャプチャの権限確認**
   - Windows: 画面キャプチャの権限が必要な場合があります

### リモート制御が動作しない場合

1. **pyautoguiライブラリの確認**
   ```bash
   pip install pyautogui
   ```

2. **マウス/キーボード制御の権限確認**
   - 一部のシステムでは、リモート制御の権限が必要な場合があります

### 認証エラーが発生する場合

1. **パスワードの確認**
   - サーバー側で表示されたパスワードが正しいか確認
   - パスワードは大文字小文字を区別します

2. **接続タイムアウト**
   - ネットワークが遅い場合、タイムアウトが発生する可能性があります
   - 再接続を試してください

## 🎓 学習ポイント

このコードから学べること：

1. **ソケット通信**: TCPソケットを使った通信
2. **SSL/TLS暗号化**: セキュアな通信の実装
3. **画面キャプチャ**: mssライブラリを使った画面キャプチャ
4. **画像処理**: PIL/Pillowを使った画像処理と圧縮
5. **差分更新**: 効率的な画面更新アルゴリズム
6. **マルチスレッド**: 非同期処理によるリアルタイム通信
7. **GUI開発**: tkinterを使ったGUIアプリケーション
8. **ファイル転送**: バイナリデータの転送
9. **クリップボード操作**: システムクリップボードとの連携

## 🔄 今後の改善案

- [ ] 音声転送機能
- [ ] 複数モニター対応
- [ ] NAT/ファイアウォール通過（STUN/TURNサーバー）
- [ ] 再接続機能の改善
- [ ] 接続履歴の記録
- [ ] 画面録画機能
- [ ] マルチクライアント対応
- [ ] 適切なSSL証明書のサポート

## 📝 ライセンス

このプロジェクトは教育目的で作成されています。

---

**注意**: 本番環境で使用する場合は、適切なセキュリティ対策を実施してください。




