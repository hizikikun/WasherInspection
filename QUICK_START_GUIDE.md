# クイックスタートガイド

## 📍 どちらで何を実行するか

### デスクトップ側（自宅のPC）
- **サーバー**を起動
- 画面を共有して、リモート制御を受け付ける

### ノートパソコン側（大学のPC）
- **クライアント**を起動
- デスクトップの画面を見て、操作する

---

## 🚀 手順（カスタムリモートデスクトップの場合）

### ステップ1: 必要なライブラリをインストール（両方のPCで実行）

#### デスクトップ側で実行:
```bash
pip install mss pillow pyautogui
```

#### ノートパソコン側で実行:
```bash
pip install mss pillow pyautogui
```

**または、requirements.txtからインストール:**
```bash
pip install -r requirements.txt
```

---

### ステップ2: デスクトップ側でサーバーを起動

#### 方法A: バッチファイルから起動（簡単）
1. エクスプローラーで `WasherInspection` フォルダを開く
2. `start_custom_remote_server.bat` をダブルクリック

#### 方法B: コマンドプロンプトから起動
1. コマンドプロンプトを開く
2. プロジェクトフォルダに移動:
   ```bash
   cd C:\Users\西村康成\WasherInspection
   ```
3. サーバーを起動:
   ```bash
   python custom_remote_desktop.py server
   ```

**表示される内容:**
```
=== リモートデスクトップサーバー ===
デスクトップ側で実行してください
クライアントの接続を待機しています...
[サーバー] 待機中: 0.0.0.0:8888
```

**この状態で待機します。** ノートパソコン側からの接続を待っています。

---

### ステップ3: デスクトップ側のIPアドレスを確認

デスクトップ側で、コマンドプロンプトを開いて:
```bash
ipconfig
```

**IPv4アドレスをメモしてください**（例: `192.168.1.100`）

---

### ステップ4: ノートパソコン側でクライアントを起動

#### 方法A: バッチファイルから起動（簡単）
1. エクスプローラーで `WasherInspection` フォルダを開く
2. `start_custom_remote_client.bat` をダブルクリック
3. サーバーIPアドレスを入力（例: `192.168.1.100`）

#### 方法B: コマンドプロンプトから起動
1. コマンドプロンプトを開く
2. プロジェクトフォルダに移動:
   ```bash
   cd C:\Users\西村康成\WasherInspection
   ```
3. クライアントを起動（IPアドレスを指定）:
   ```bash
   python custom_remote_desktop.py client 192.168.1.100
   ```
   （`192.168.1.100` の部分を、ステップ3で確認したIPアドレスに変更）

#### 方法C: GUIモードで起動
1. コマンドプロンプトを開く
2. プロジェクトフォルダに移動:
   ```bash
   cd C:\Users\西村康成\WasherInspection
   ```
3. GUIモードで起動:
   ```bash
   python custom_remote_desktop.py
   ```
4. 「クライアント（ノートパソコン側）」をクリック
5. サーバーIPアドレスを入力して「接続」をクリック

---

### ステップ5: 接続完了

ノートパソコン側にデスクトップの画面が表示されれば成功です！

- **マウス操作**: 画面内でマウスを動かすと、デスクトップ側のマウスが動きます
- **クリック**: 画面をクリックすると、デスクトップ側でクリックされます
- **キーボード**: キーを押すと、デスクトップ側で入力されます

---

## 📋 まとめ表

| PC | 実行するコマンド | 説明 |
|---|---|---|
| **デスクトップ側** | `python custom_remote_desktop.py server` | サーバーを起動（接続待機） |
| **ノートパソコン側** | `python custom_remote_desktop.py client [IPアドレス]` | クライアントを起動（接続） |

---

## 🔧 トラブルシューティング

### 接続できない場合

1. **IPアドレスが正しいか確認**
   - デスクトップ側で `ipconfig` を実行して確認

2. **ファイアウォールの確認**
   - Windowsファイアウォールでポート8888がブロックされていないか確認
   - 必要に応じてポートを開放

3. **同じネットワークに接続されているか確認**
   - 両方のPCが同じWiFiまたは有線LANに接続されている必要があります

### 画面が表示されない場合

1. **ライブラリがインストールされているか確認**
   ```bash
   pip list | findstr mss
   pip list | findstr pyautogui
   ```

2. **エラーメッセージを確認**
   - コマンドプロンプトに表示されるエラーメッセージを確認

---

## 💡 簡単な使い方（バッチファイル使用）

### デスクトップ側:
1. `start_custom_remote_server.bat` をダブルクリック
2. 待機（接続を待つ）

### ノートパソコン側:
1. `start_custom_remote_client.bat` をダブルクリック
2. IPアドレスを入力（例: `192.168.1.100`）
3. Enterキーを押す

これで接続できます！






