# セットアップ手順（詳細版）

## 🎯 全体の流れ

```
[デスクトップ側]                    [ノートパソコン側]
     ↓                                      ↓
1. ライブラリインストール         1. ライブラリインストール
     ↓                                      ↓
2. サーバー起動                   2. IPアドレス確認
     ↓                                      ↓
3. 接続待機 ←─────────────────── 3. クライアント起動
     ↓                                      ↓
4. 画面共有・リモート制御受付     4. 画面表示・リモート制御送信
```

---

## 📍 デスクトップ側（自宅のPC）で実行すること

### 1. 必要なライブラリをインストール

**コマンドプロンプトを開いて:**
```bash
cd C:\Users\西村康成\WasherInspection
pip install mss pillow pyautogui
```

**実行結果:**
```
Successfully installed mss-9.0.1 pillow-10.1.0 pyautogui-0.9.54
```

### 2. IPアドレスを確認

**コマンドプロンプトで:**
```bash
ipconfig
```

**表示例:**
```
IPv4 アドレス . . . . . . . . . . . . : 192.168.1.100
```

**このIPアドレスをメモしてください！** ノートパソコン側で使用します。

### 3. サーバーを起動

**方法A: バッチファイル（簡単）**
- `start_custom_remote_server.bat` をダブルクリック

**方法B: コマンドプロンプト**
```bash
cd C:\Users\西村康成\WasherInspection
python custom_remote_desktop.py server
```

**表示される内容:**
```
=== リモートデスクトップサーバー ===
デスクトップ側で実行してください
クライアントの接続を待機しています...
[サーバー] 待機中: 0.0.0.0:8888
```

**この状態で待機します。** ノートパソコン側からの接続を待っています。

### 4. 接続が確立されたら

```
[サーバー] クライアント接続: ('192.168.1.2', 54321)
```

このメッセージが表示されれば、接続成功です！

---

## 📍 ノートパソコン側（大学のPC）で実行すること

### 1. 必要なライブラリをインストール

**コマンドプロンプトを開いて:**
```bash
cd C:\Users\西村康成\WasherInspection
pip install mss pillow pyautogui
```

### 2. デスクトップ側のIPアドレスを確認

デスクトップ側で確認したIPアドレスをメモしてください。
（例: `192.168.1.100`）

### 3. クライアントを起動

**方法A: バッチファイル（簡単）**
1. `start_custom_remote_client.bat` をダブルクリック
2. プロンプトが表示されたら、IPアドレスを入力:
   ```
   サーバーIPアドレスを入力してください (デフォルト: 192.168.1.100): 192.168.1.100
   ```
3. Enterキーを押す

**方法B: コマンドプロンプト（直接指定）**
```bash
cd C:\Users\西村康成\WasherInspection
python custom_remote_desktop.py client 192.168.1.100
```
（`192.168.1.100` の部分を、デスクトップ側のIPアドレスに変更）

**方法C: GUIモード**
```bash
cd C:\Users\西村康成\WasherInspection
python custom_remote_desktop.py
```
1. 「クライアント（ノートパソコン側）」をクリック
2. サーバーアドレスにIPアドレスを入力（例: `192.168.1.100`）
3. 「接続」ボタンをクリック

### 4. 接続成功

GUIウィンドウが開き、デスクトップの画面が表示されれば成功です！

- **マウス操作**: 画面内でマウスを動かすと、デスクトップ側のマウスが動きます
- **クリック**: 画面をクリックすると、デスクトップ側でクリックされます
- **キーボード**: キーを押すと、デスクトップ側で入力されます

---

## 📝 コマンド一覧

### デスクトップ側

```bash
# 1. ライブラリインストール
pip install mss pillow pyautogui

# 2. IPアドレス確認
ipconfig

# 3. サーバー起動
python custom_remote_desktop.py server
```

### ノートパソコン側

```bash
# 1. ライブラリインストール
pip install mss pillow pyautogui

# 2. クライアント起動（IPアドレスを指定）
python custom_remote_desktop.py client 192.168.1.100
```

---

## 🎬 実際の使用例

### 例: デスクトップのIPアドレスが `192.168.1.100` の場合

#### デスクトップ側:
```bash
C:\Users\西村康成\WasherInspection> python custom_remote_desktop.py server
=== リモートデスクトップサーバー ===
デスクトップ側で実行してください
クライアントの接続を待機しています...
[サーバー] 待機中: 0.0.0.0:8888
[サーバー] クライアント接続: ('192.168.1.2', 54321)
```

#### ノートパソコン側:
```bash
C:\Users\西村康成\WasherInspection> python custom_remote_desktop.py client 192.168.1.100
=== リモートデスクトップクライアント ===
サーバー: 192.168.1.100:8888
```

GUIウィンドウが開き、デスクトップの画面が表示されます。

---

## ⚠️ 注意事項

1. **同じネットワークに接続されている必要があります**
   - 両方のPCが同じWiFiまたは有線LANに接続されている必要があります
   - 大学のWiFiから自宅に接続する場合は、ポートフォワーディングが必要な場合があります

2. **ファイアウォールの設定**
   - ポート8888がブロックされている場合は、ファイアウォールで開放する必要があります

3. **IPアドレスが変わることがあります**
   - DHCPを使用している場合、IPアドレスが変わる可能性があります
   - その場合は、再度 `ipconfig` で確認してください

---

## 🔄 切断方法

### ノートパソコン側:
- GUIウィンドウの「切断」ボタンをクリック
- または、ウィンドウを閉じる

### デスクトップ側:
- `Ctrl + C` でサーバーを停止

---

## 📞 サポート

問題が発生した場合は、以下を確認してください:

1. エラーメッセージを確認
2. `QUICK_START_GUIDE.md` を参照
3. `CUSTOM_REMOTE_DESKTOP_README.md` を参照






