# デスクトップ側の完全な操作手順

## 📋 デスクトップ側で必要な操作（最初から最後まで）

---

## 🖥️ ステップ1: フォルダを作成

### 手順

1. **エクスプローラーを開く**
   - **Windowsキー + E** を同時に押す
   - または、タスクバーのフォルダアイコンをクリック

2. **デスクトップに移動**
   - 左側のサイドバーで「**デスクトップ**」をクリック

3. **新しいフォルダを作成**
   - デスクトップの空白部分を**右クリック**
   - 「**新規作成**」→「**フォルダ**」を選択
   - フォルダ名を入力（例: `RemoteDesktop`）
   - **Enterキー**を押す

---

## 📁 ステップ2: 必要なファイルをコピー

### 手順

1. **WasherInspectionフォルダを開く**
   - エクスプローラーで `C:\Users\西村康成\WasherInspection` を開く
   - 左側のサイドバーで「PC」→「ローカルディスク(C:)」→「Users」→「西村康成」→「WasherInspection」をクリック

2. **ファイルをコピー**
   - `custom_remote_desktop.py` を**右クリック** → 「**コピー**」
   - `start_custom_remote_server.bat` を**右クリック** → 「**コピー**」

3. **作成したフォルダに貼り付け**
   - デスクトップの `RemoteDesktop` フォルダを**ダブルクリック**して開く
   - フォルダ内の空白部分を**右クリック** → 「**貼り付け**」

4. **ファイルがコピーされたか確認**
   - フォルダ内に以下のファイルがあるか確認:
     - ✅ `custom_remote_desktop.py`
     - ✅ `start_custom_remote_server.bat`

---

## 💻 ステップ3: コマンドプロンプトを開く

### 方法A: エクスプローラーから開く（簡単・推奨）

1. **エクスプローラーでフォルダを開く**
   - デスクトップの `RemoteDesktop` フォルダを**ダブルクリック**して開く

2. **アドレスバーに「cmd」と入力**
   - フォルダを開いた状態で、上部の**アドレスバー**（パスが表示されている場所）を**クリック**
   - 「**cmd**」と入力して**Enterキー**を押す

3. **コマンドプロンプトが開く**
   - そのフォルダでコマンドプロンプトが開きます
   - パスが `C:\Users\西村康成\Desktop\RemoteDesktop>` のように表示されます

### 方法B: Windowsキー + Rから開く

1. **Windowsキー + R** を同時に押す
2. 「**cmd**」と入力して**Enterキー**を押す
3. コマンドプロンプトが開きます
4. フォルダに移動:
   ```bash
   cd "C:\Users\西村康成\Desktop\RemoteDesktop"
   ```
   - **Enterキー**を押す

---

## 📦 ステップ4: ライブラリをインストール

### 手順

1. **コマンドプロンプトに以下を入力**
   ```bash
   python -m pip install mss pillow pyautogui
   ```

2. **Enterキーを押す**

3. **インストールが完了するまで待つ**
   - 画面に以下のように表示されます:
     ```
     Collecting mss
       Downloading mss-9.0.1-py3-none-any.whl
     ...
     Successfully installed mss-9.0.1 pillow-10.1.0 pyautogui-0.9.54
     ```

4. **インストールが完了したら、次のステップに進む**

---

## 🔍 ステップ5: IPアドレスを確認

### 手順

1. **コマンドプロンプトに以下を入力**
   ```bash
   ipconfig
   ```

2. **Enterキーを押す**

3. **IPv4アドレスを確認**
   - 画面に以下のように表示されます:
     ```
     Windows IP構成

     イーサネット アダプター イーサネット:

         IPv4 アドレス . . . . . . . . . . . : 192.168.1.100
     ```

4. **IPv4アドレスの数字をメモ**
   - 例: `192.168.1.100`
   - **このIPアドレスをノートパソコン側で使用します**

---

## 🚀 ステップ6: サーバーを起動

### 方法A: バッチファイルから起動（簡単）

1. **エクスプローラーでフォルダを開く**
   - デスクトップの `RemoteDesktop` フォルダを開く

2. **バッチファイルを実行**
   - `start_custom_remote_server.bat` を**ダブルクリック**

3. **コマンドプロンプトウィンドウが開く**
   - サーバーが起動します

### 方法B: コマンドプロンプトから起動

1. **コマンドプロンプトに以下を入力**
   ```bash
   python custom_remote_desktop.py server
   ```

2. **Enterキーを押す**

3. **サーバーが起動する**
   - 画面に以下のように表示されます:
     ```
     === リモートデスクトップサーバー ===
     デスクトップ側で実行してください
     クライアントの接続を待機しています...
     [サーバー] 待機中: 0.0.0.0:8888
     ```

---

## ⏳ ステップ7: 接続を待機

### 手順

1. **「[サーバー] 待機中: 0.0.0.0:8888」と表示されていることを確認**
   - この状態で待機します

2. **このコマンドプロンプトウィンドウは閉じないでください**
   - サーバーが動作している間、このウィンドウを開いたままにしておく必要があります

3. **ノートパソコン側から接続されるのを待つ**
   - 接続されると、以下のように表示されます:
     ```
     [サーバー] クライアント接続: ('192.168.1.2', 54321)
     ```

---

## 📋 デスクトップ側のチェックリスト

- [ ] フォルダを作成した（`RemoteDesktop`）
- [ ] `custom_remote_desktop.py` をコピーした
- [ ] `start_custom_remote_server.bat` をコピーした
- [ ] コマンドプロンプトを開いた
- [ ] ライブラリをインストールした（`python -m pip install mss pillow pyautogui`）
- [ ] IPアドレスを確認した（`ipconfig`）
- [ ] IPv4アドレスをメモした（例: `192.168.1.100`）
- [ ] サーバーを起動した（`python custom_remote_desktop.py server`）
- [ ] 「[サーバー] 待機中: 0.0.0.0:8888」と表示されている
- [ ] コマンドプロンプトウィンドウを開いたままにしている

---

## 🔴 エラーが発生した場合

### エラー1: 「'python' は、内部コマンドまたは外部コマンド...」

**対処法:**
1. Pythonをインストール: https://www.python.org/downloads/
2. インストール時に「**Add Python to PATH**」にチェックを入れる
3. コマンドプロンプトを再起動

### エラー2: 「ModuleNotFoundError: No module named 'mss'」

**対処法:**
```bash
python -m pip install mss pillow pyautogui
```

### エラー3: ファイルが見つからない

**対処法:**
1. ファイルが正しいフォルダにあるか確認
2. ファイル名が正しいか確認（`custom_remote_desktop.py`）

---

## 💡 ヒント

### サーバーを起動した後

- **コマンドプロンプトウィンドウを閉じないでください**
- サーバーが動作している間、このウィンドウを開いたままにしておく必要があります
- ウィンドウを閉じると、サーバーが停止します

### IPアドレスが変わることがある

- DHCPを使用している場合、IPアドレスが変わる可能性があります
- その場合は、再度 `ipconfig` で確認してください

---

## 📝 まとめ

デスクトップ側で必要な操作:

1. ✅ フォルダを作成
2. ✅ ファイルをコピー
3. ✅ コマンドプロンプトを開く
4. ✅ ライブラリをインストール
5. ✅ IPアドレスを確認
6. ✅ サーバーを起動
7. ✅ 接続を待機

**これで、デスクトップ側の準備は完了です！**





