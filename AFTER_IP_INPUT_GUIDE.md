# IPアドレス入力後の対処法

## 📍 現在の状態

IPアドレスを入力した後、画面に以下のように表示されているはずです:

```
Python Press any key to continue . . . |
```

これは、バッチファイルが実行完了した後に表示されるメッセージです。

---

## 🎯 次のステップ

### ステップ1: 何かキーを押す

画面に「Press any key to continue...」と表示されている場合:

1. **何かキーを押す**（Enterキー、スペースキーなど）
2. コマンドプロンプトウィンドウが閉じます

### ステップ2: GUIウィンドウが開いているか確認

IPアドレスを入力した後、**別のウィンドウ（GUIウィンドウ）が開いているはずです**。

#### 正常な場合

1. **リモートデスクトップのGUIウィンドウが開いている**
   - タイトル: 「リモートデスクトップ - クライアント」
   - デスクトップ側の画面が表示されている

2. **接続状態が表示されている**
   - 上部に「接続中: 192.168.1.100:8888」または「接続成功: 192.168.1.100」と表示

#### GUIウィンドウが開いていない場合

エラーが発生している可能性があります。以下の手順で確認してください。

---

## 🔴 エラーが発生した場合

### エラー1: GUIウィンドウが開かない

#### 原因
- Pythonが正しくインストールされていない
- ライブラリがインストールされていない
- 接続エラー

#### 対処法

1. **コマンドプロンプトを再度開く**
   - エクスプローラーでデスクトップのフォルダを開く
   - アドレスバーに「cmd」と入力してEnter

2. **直接Pythonコマンドで実行**
   ```bash
   python custom_remote_desktop.py client 192.168.1.100
   ```
   （`192.168.1.100` を実際のIPアドレスに変更）

3. **エラーメッセージを確認**
   - コマンドプロンプトにエラーメッセージが表示されます
   - エラーメッセージの内容を確認してください

### エラー2: 「サーバーに接続できませんでした」

#### 原因
- デスクトップ側でサーバーが起動していない
- IPアドレスが間違っている
- ファイアウォールがブロックしている

#### 対処法

1. **デスクトップ側でサーバーが起動しているか確認**
   - デスクトップ側のコマンドプロンプトに「[サーバー] 待機中: 0.0.0.0:8888」と表示されているか確認

2. **IPアドレスが正しいか確認**
   - デスクトップ側で再度 `ipconfig` を実行して確認

3. **ファイアウォールの設定を確認**
   - Windowsファイアウォールでポート8888がブロックされていないか確認

---

## 💡 正常な動作の確認

### 正常に接続できた場合の画面

1. **GUIウィンドウが開く**
   - タイトル: 「リモートデスクトップ - クライアント」
   - サイズ: 1024x768（デフォルト）

2. **接続状態の表示**
   - 上部に「接続中: 192.168.1.100:8888」と表示
   - その後、「接続成功: 192.168.1.100」と表示

3. **デスクトップの画面が表示される**
   - ウィンドウ内に、デスクトップ側の画面が表示されます
   - リアルタイムで更新されます

### 操作方法

- **マウス操作**: ウィンドウ内でマウスを動かすと、デスクトップ側のマウスが動きます
- **クリック**: 画面をクリックすると、デスクトップ側でクリックされます
- **キーボード**: キーを押すと、デスクトップ側で入力されます

---

## 🔍 トラブルシューティング

### 方法1: コマンドプロンプトから直接実行

バッチファイルではなく、コマンドプロンプトから直接実行してみてください:

1. **コマンドプロンプトを開く**
   - エクスプローラーでデスクトップのフォルダを開く
   - アドレスバーに「cmd」と入力してEnter

2. **クライアントを起動**
   ```bash
   python custom_remote_desktop.py client 192.168.1.100
   ```
   （`192.168.1.100` を実際のIPアドレスに変更）

3. **エラーメッセージを確認**
   - エラーが出た場合は、メッセージを確認してください

### 方法2: ライブラリがインストールされているか確認

```bash
python -m pip list | findstr mss
python -m pip list | findstr pyautogui
```

表示されない場合は、インストールしてください:

```bash
python -m pip install mss pillow pyautogui
```

---

## 📝 まとめ

1. ✅ IPアドレスを入力した
2. ✅ 「Press any key to continue...」と表示された
3. ✅ **何かキーを押す**（Enterキーなど）
4. ✅ **GUIウィンドウが開いているか確認**
   - 開いている → 接続成功！
   - 開いていない → エラーが発生している可能性があります

### GUIウィンドウが開いていない場合

コマンドプロンプトから直接実行して、エラーメッセージを確認してください:

```bash
python custom_remote_desktop.py client 192.168.1.100
```

エラーメッセージを教えていただければ、さらに詳しくサポートできます。





